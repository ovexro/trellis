# Trellis — Feature Inventory

Single source of truth for all features. Check items as they are implemented and tested.

## Desktop App

### Discovery & Connection
- [x] mDNS device scanning on local network
- [x] Auto-refresh device list (continuous mDNS + health check)
- [x] Device online/offline detection
- [x] Manual IP entry fallback
- [x] USB serial device detection (listing)
- [x] **Saved devices auto-load on app restart (v0.3.3)** — `Discovery::hydrate_from_db` reads `SavedDevice` rows from SQLite into the in-memory map at startup as offline placeholders, so cross-subnet devices added by IP reappear instantly on every consumer (desktop UI, REST API, web dashboard, MQTT bridge). Health check loop restructured to "work, then sleep" so the first probe runs immediately and hydrated devices flip online within ~1 second instead of waiting a 30-second interval.

### Home Overview (post-v0.6.0)
- [x] **Home page as default landing view** — new Home page in both desktop app (React, `/` route) and web dashboard (`:9090`, first tab). Shows system status strip (online/offline device counts, MQTT/Sinric bridge status dots), live sensor readings grid (all sensor capabilities across all devices with current values), quick controls (functional inline switches, sliders, color pickers across all devices — offline devices dimmed), and recent activity feed (last 30 cross-device events from `GET /api/activity` endpoint, severity color-coded, clickable to navigate to device). Real-time updates via Zustand store (desktop) and WebSocket push (web dashboard). Activity feed backed by new `idx_logs_timestamp` index for efficient cross-device queries.
- [x] **Per-capability favorites / pinning (post-v0.6.0)** — star toggle on every sensor and control card in the Home page (desktop app and web dashboard). Clicking the star pins that specific capability to a "Favorites" section at the top of the Home page; non-favorited capabilities remain in the regular "Live Readings" and "Quick Controls" sections below (no duplication). Favorites persisted in SQLite (`favorite_capabilities` table with device_id + capability_id composite key). Tauri command `toggle_favorite_capability` + REST API `POST /api/favorites/toggle` and `GET /api/favorites` endpoints. Admin-only toggle (viewers see stars but cannot click). Optimistic UI with revert on failure in both desktop (Zustand store) and web dashboard. Amber filled star for active, gray outline for inactive.

### Floor Plan (post-v0.6.0)
- [x] **Spatial device layout** — new Floor Plan page in both desktop app (React, `/floor-plan` route, Map icon in sidebar) and web dashboard (`:9090`, new tab). Freeform canvas where devices are dragged from a sidebar panel and placed at percentage-based (x, y) positions. Placed nodes show live status dot (online/offline), primary capability value (sensor reading, switch state), and device name. Click a node to open an inline popup with all sensor readings and interactive controls (switches, sliders) — sends commands via existing `send_command` pipeline. Drag nodes to reposition; positions persisted in SQLite (`device_positions` table). Background image support per floor. Server-side x/y clamping to 0–100. Web dashboard includes touch support for mobile drag. Device deletion cascades to positions and favorites. Viewers can see the floor plan but cannot move devices or change background.
- [x] **Multi-floor support (post-v0.7.0)** — new `floor_plans` table (id, name, sort_order, background). Tab bar above the canvas lists all floors; click to switch, `+` button to add, right-click for rename/delete context menu. Each floor has its own device positions and background image. Sidebar shows only unplaced devices (not on any floor). Background moved from global settings to per-floor storage. Seamless migration: existing positions and background move to an auto-created "Floor 1". REST API: `GET /api/floor-plans`, `POST /api/floor-plans` (admin), `PUT /api/floor-plans/{id}` (admin), `DELETE /api/floor-plans/{id}` (admin, cascades positions). `GET /api/floor-plan` accepts `?floor_id=N` (defaults to first floor). `PUT /api/floor-plan/position` now requires `floor_id`. Both desktop and web dashboard.
- [x] **Snap-to-grid toggle (post-v0.7.0)** — toggle button in the sidebar snaps device positions to a 4% grid (aligned with the 32px dot pattern). Grid dots turn trellis-accent when enabled and remain visible over background images. Applied to sidebar drops, node drags (mouse + touch). Both desktop and web dashboard.
- [x] **Compact labels toggle (post-v0.7.0)** — toggle in the sidebar switches placed nodes between expanded (name + value, default) and compact (value only, smaller padding). Reduces clutter on crowded floor plans. Both desktop and web dashboard.
- [x] **Undo last move (post-v0.7.0)** — Ctrl+Z / Cmd+Z reverts the last floor plan action: new placement (undo removes), move (undo restores previous position), or removal (undo restores device). Single-level. Both desktop and web dashboard.
- [x] **Rooms (post-v0.10.1)** — named rectangular regions on the floor plan. New `floor_plan_rooms` table (id, floor_id, name, color, x/y/w/h percentages, sort_order) cascades from `floor_plans` on delete. Add Room button spawns a 30×25% rectangle in the top-left area with the next color from an 8-swatch palette. Rooms render beneath devices (z-index 5) with a dashed border when unselected and a solid border + drag-resize handle when selected. Drag the body to move (clamped so the rectangle can't escape the canvas); drag the SE handle to resize (min 5%, max whatever keeps the rectangle inside). Sidebar edit panel for the selected room: rename input (persists on blur / Enter), 8-swatch color picker (instant persist), delete button. REST endpoints: `GET /api/floor-plans/:floor_id/rooms`, `POST /api/floor-plans/:floor_id/rooms` (admin), `PUT /api/rooms/:id` (admin), `DELETE /api/rooms/:id` (admin). Tauri commands `get_rooms`/`create_room`/`update_room`/`delete_room`. Server clamps geometry so `x+w ≤ 100` and `y+h ≤ 100`. Both desktop React app and embedded web UI. Polygons, rotation, walls, and auto-placement deferred to a future Floor Plan v3 pending user demand.
- [x] **Device room indicator (post-v0.10.1)** — placed devices inside a room rectangle show the room name as a subtle label beneath the device name. Pure derived property computed on each render by checking if the device's (x, y) falls within any room's (x, y, w, h) bounds — no schema change, no stored association. Updates instantly when a device is dragged into/out of a room or when a room is moved/resized. Hidden in compact mode. Both desktop React app and embedded web UI.

### Dashboard
- [x] Device card grid layout
- [x] **Drag-and-drop card reordering (post-v0.4.4)** — device cards can be reordered via drag-and-drop in both the desktop app and the `:9090` web dashboard. Order persists in SQLite (`sort_order` column on `devices` table). `PUT /api/devices/reorder` REST endpoint (admin-only). Viewers see cards in the same order but cannot drag. HTML5 DnD with visual feedback (opacity on drag, accent ring on drop target, grip handle icon).
- [x] Device card: name, status indicator, RSSI, uptime, firmware version, chip type
- [x] Click card → device detail view
- [x] Device grouping / tagging (nicknames, tags, pills on cards)
- [x] Search / filter devices (by name, nickname, IP, platform, chip, tags, notes, install_date) — parity on desktop Dashboard + embedded `:9090` web UI as of post-v0.20.0; notes/install_date joined via cached savedDevices; empty-match state shows "No devices match …"
- [x] Device rooms/groups (create groups, assign devices, grouped dashboard view)
- [x] Group management UI (create, edit, delete, color palette)
- [x] **Room-filtered dashboard view (post-v0.11.0)** — pill-shaped chips above the device grid narrow the view to a single floor-plan room ("Kitchen", "Living Room", …), "Unplaced" for devices without a floor-plan position or outside every room, or "All" to clear. Room membership is pure derived state: the dashboard loads all rooms (`get_all_rooms` Tauri command / `GET /api/rooms`) and all positions (`get_all_device_positions` / `GET /api/floor-plan/positions`) once, then point-in-rectangle tests each device's (floor_id, x, y) against the rooms on that floor — no schema change, no stored device↔room association. Chips only render when at least one room contains a device, and each chip shows the room's color dot and current device count. Both desktop React app and embedded `:9090` web UI. Unblocks room-scoped scenes ("turn on all lights in Greenhouse") as a follow-up.
- [x] **Scene from room (post-v0.11.0)** — when a single-room chip filter is active on the Devices tab, a "✨ Scene from {Room}" button opens a scaffolder modal that builds a scene targeting every controllable device currently inside that room. Supports three capability-type templates (Switches On/Off, Sliders 0–100, Colors via hex picker) that can be combined; each template expands across all matching capabilities in the room. Live count shows how many actions and devices the scene will cover. When no controllable capabilities are found (e.g., every device in the room is offline, since capability descriptors are only read while online), the modal shows an amber explanation instead of blocking the user. On save, calls the existing `create_scene` command and navigates to the Scenes page with the new scene at the top. Desktop React app and (post-v0.12.0) embedded `:9090` web dashboard — the web version uses the existing `POST /api/scenes` REST endpoint and tab-switches to the Scenes tab on success. Admin-only in both environments (viewers see chips but not the scaffolder button).
- [x] **Inline controls and live values on device cards (post-v0.9.0)** — device cards in both the desktop app and web dashboard now show live capability data directly without expanding or navigating. Desktop cards display interactive switch toggles, compact sliders with debounced command sending, sensor values with units, color swatches with hex values, and truncated text values. Switches and sliders are fully interactive on online devices (offline devices show dimmed controls). Web dashboard collapsed cards show a capability preview row with compact switch toggles, sensor/slider values, color swatches, and text previews; preview auto-hides when the card is expanded to avoid duplication. Event propagation handled so clicking controls doesn't trigger card navigation/expansion. Outer card element changed from `<button>` to `<div role="button">` to allow valid nested interactive elements.

### Auto-Generated Controls
- [x] Switch (toggle) — maps to `type: "switch"`
- [x] Slider (range) — maps to `type: "slider"`
- [x] Sensor (read-only gauge/value) — maps to `type: "sensor"`
- [x] Color picker — maps to `type: "color"`
- [x] Text display — maps to `type: "text"`
- [x] Controls update in real-time via WebSocket

### Charts & Metrics
- [x] Time-series chart for sensor data
- [x] System metrics panel (RSSI, heap charts — shown for all devices)
- [x] Metric history stored in SQLite
- [x] Configurable chart time range
- [x] **Metrics page (post-v0.4.9)** — top-level monitoring overview in the desktop app showing all devices with uptime ribbon, RSSI/heap/sensor charts in a 2-column grid, global time range picker (1h/6h/24h/7d), device status indicators, and "Details" links. Reuses existing MetricChart and UptimeTimeline components via new `externalHours` prop that locks the range and hides per-chart pickers. Matches the `:9090` web dashboard Metrics tab layout.
- [x] **Reset-event chart annotations (v0.18.0)** — each row in `device_reset_history` now surfaces as a vertical marker on every per-device metric chart, completing the loop started by v0.17.0's `power_supply_stability` rule (rule flags the cluster, chart annotation shows when each reboot happened). `db.get_annotations` gains a third UNION-style pass over `device_reset_history`, bucketing `reset_reason` into three kinds that track the rule's taxonomy: **`reset_brownout`** (error severity, strong red `#dc2626`) for `brownout`; **`reset_fault`** (warn severity, purple `#a855f7`) for `panic` / `watchdog` / `task_watchdog` / `interrupt_watchdog`; **`reset`** (info severity, slate `#64748b`) for expected/ambiguous reasons (`poweron`, `software`, `external`, `deepsleep`, `unknown` via fallback). Humanized labels ("Brownout reset", "Watchdog reset", "Power-on reset", …) render in the SVG `<title>` hover tooltip. Both desktop `MetricChart.tsx` and embedded `:9090` web UI (`web_ui.html::annColor`/`annLabel`) extend their color maps and legend order by the three kinds — new entries appear in the legend only when present in the window, matching the existing kinds' pattern. Reset annotations are click-inert (they short-circuit in both `DeviceDetail.tsx::handleAnnotationClick` and web `onAnnotationClick`) because they don't have a corresponding `device_logs` row to scroll to — the timeline position is the useful signal. Hardware-tested live against trellis-fccfb7c8 via three synthetic rows (brownout/watchdog/panic injected at T−15m/−30m/−45m alongside the real `poweron` at T−5h); all four markers rendered with the right colors, legend badges appeared, synthetic rows cleaned up immediately after. Banks into v0.18.0 alongside other Diagnostics v5 follow-ups rather than cutting a v0.17.1 patch for one visual change.

### Serial Monitor
- [x] List USB serial ports
- [x] Connect to serial port (baud rate selection)
- [x] Send/receive text
- [x] Auto-scroll with pause
- [x] Clear buffer
- [x] Copy to clipboard

### Device Diagnostics (v0.12.0)
- [x] **One-click health check per device** — new Diagnostics section on the device detail panel (desktop React app) and embedded `:9090` web UI, click "Run diagnostics" to get a plain-English verdict on WiFi signal, free memory, memory leak pattern, uptime %, reconnect count, error rate, and firmware age. Pure read-only aggregation over the last 24h of stored `_rssi` / `_heap` metrics, `device_logs` state transitions + error/warn rows, and `firmware_history`. No schema change. Rule catalog lives in `src-tauri/src/diagnostics.rs` with 18 unit tests covering each rule's pass/warn/fail boundary. Overall verdict rolls up to `good` / `attention` / `unhealthy` based on whether any rule returned `fail` / `warn`. Viewer-safe: new `diagnose_device` Tauri command + `GET /api/devices/:id/diagnose` REST endpoint both read-only. Each finding carries an `id`, `level`, `title`, `detail`, and optional human-readable `suggestion` that links the symptom to a likely cause ("43 reconnects + strong RSSI" → firmware/power instability, not weak signal). Falls back to live in-memory `SystemInfo` when no historical samples exist yet (e.g. a freshly discovered device), so the first-run experience is never empty.
- [x] **Fleet Health widget on Home (post-v0.12.0)** — at-a-glance rollup of per-device diagnostics on the Home page (both desktop React app and `:9090` web dashboard). Three color-coded tiles (`Healthy` / `Attention` / `Unhealthy`) with device counts; clicking a non-empty tile expands a list of the devices in that bucket, each with critical/warning counts and an offline badge. Click any device row to drill in. Widget picks the most-urgent non-empty bucket as the default filter on load. New `diagnose_fleet` Tauri command + `GET /api/diagnostics/fleet` REST endpoint (viewer-safe) iterate saved devices via `db.get_all_saved_devices()` and reuse the per-device `diagnose` engine — zero new rules, zero schema changes. Response is sorted most-urgent-first so the UI can take a top slice. Per-device errors are silently skipped so one bad row can't hide the rest. Loads after the fast Home render (web UI) and via `useEffect` on mount (desktop).
- [x] **Fleet Health drill-in: top finding inline (post-v0.12.0)** — each device row in the Fleet Health widget now surfaces its most urgent finding (title + detail) inline, severity-colored — no click-through required to see *why* a device is flagged. `FleetDeviceEntry` gains a `top_finding: Option<TopFinding { level, title, detail }>` field picked by `pick_top_finding(&findings)` (first FAIL, else first WARN, else None — OK/INFO never surface). Iteration order follows the rule catalog in `diagnose` (online → rssi → heap → uptime → reconnect → errors → firmware), so "offline" beats "heap low" beats "reconnect flap" etc. Three new unit tests (22 total) lock the selection semantics. Replaces the earlier `N critical, M warn` counts on both desktop (`FleetHealth.tsx`) and web UI (`renderFleetHealth` in `web_ui.html`). Full detail available as a tooltip; overflow truncated with ellipsis.
- [x] **Firmware auto-remediation (v0.14.0)** — the firmware_age rule now escalates from INFO to WARN when a newer release is published in the device's bound GitHub repo, and exposes a one-click "Update to {tag}" button in the per-device Diagnostics card. Finding gains an optional structured `action { label, action_type, data }` slot (`firmware_update` action carries `release_tag`, `asset_name`, `download_url`). Each device can be bound to an `owner/repo` via a collapsible panel at the top of the Diagnostics section (desktop + web); the binding lives on two new columns on the `devices` table. The rule engine stays synchronous: callers (`diagnose_device`, `diagnose_fleet`, REST equivalents) pre-fetch eligible releases via a blocking `ureq` helper that dedupes by (owner, repo) so a 20-device fleet on one repo makes a single GitHub API call. Version comparison tolerates `v` prefix and `-prerelease` / `+build` suffixes; anything unparseable short-circuits to "skip" so the rule never nags about versions it can't compare. Desktop button invokes existing `start_github_ota`; web UI renders an equivalent one-click button in the Diagnostics finding row that POSTs to the existing admin-gated `POST /api/github/ota` endpoint (reuses the blocking download + `ota::serve_firmware` path and the `gh_download_progress` WebSocket broadcast already shared with the OTA Manager tab). New tests: `parse_version` tolerance and rejection, `is_newer_version` semver compare, `check_firmware_age` with/without eligible update and with unknown current version (22 → 30 tests).
- [x] **Error rate trend (v0.14.0)** — new `error_rate_trend` rule differentiates "errors are still firing right now" from the existing `error_rate` rule's 24h total. Splits the same `error`/`warn` log set into last-hour vs preceding 23h, computes a per-hour baseline, and surfaces `fail` at ≥10 events/hr with ≥3× baseline (or any 10+ with zero baseline), `warn` at ≥5/hr with ≥2× baseline, else `ok`. Detail string reads either "N events in last hour vs M.M/h baseline (X.Xx)" or "N events in last hour with no prior events in 23h window." Pure read from `device_logs` — no schema change, no UI change (rule renders generically via both desktop `DeviceDiagnostics.tsx` and web `renderDiagFinding`). Verified live: on trellis-fccfb7c8 (110 errors + 109 warns over 24h, 0 in last hour), `error_rate` correctly stays FAIL while `error_rate_trend` correctly reads OK with "9.5/h baseline." 6 new unit tests (silent / zero-baseline spike / 3× spike / moderate spike / sub-threshold / steady noise) — 30 → 36 total.
- [x] **OTA delivery success rate (post-v0.14.0)** — new `ota_success_rate` rule reads the ratio of successful vs failed OTA uploads over the last 10 recorded outcomes and surfaces `fail` at <50% delivered, `warn` at <80%, else `ok`. Stays at INFO ("N OTA outcomes recorded so far, need 3 for trend") until 3 attempts have been recorded, so a fresh device isn't fail-flagged on a single bad upload. Backed by two new nullable columns on `firmware_history` (`delivery_status`, `delivered_at`) — `ota::serve_firmware` calls `db.mark_firmware_delivery(device_id, "delivered" | "failed")` right before emitting the `ota_delivered` / `ota_delivery_failed` Tauri event, so outcome survives restart. Pre-v0.15.0 rows have NULL `delivery_status` and the rule skips them, only earning trust as new uploads accumulate. Persistence is best-effort: a DB write failure logs warn but never propagates into the OTA flow. Verified live on trellis-fccfb7c8 with three real `/api/github/ota` round-trips — three `failed` outcomes durably recorded (sqlite3 read of `trellis.db` confirms), and the rule tipped from INFO ("0 outcomes recorded") to FAIL ("0/3 delivered, 0%") with the right suggestion. 7 new unit tests (no recorded outcomes / below min_samples / OK at 90% / WARN at 70% / FAIL at 40% / 10-sample window cap / NULL-row skipping) — 36 → 43 total.
- [x] **OTA delivery error persistence (post-v0.14.0)** — the ratio rule above now has diagnosis-grade detail. `firmware_history` gains a third nullable column `delivery_error` that captures the same failure-category string `serve_firmware` was already emitting on `ota_delivery_failed` Tauri events (e.g. `"accept: timed out"`, `"body: Broken pipe (os error 32)"`, `"flush: Connection reset by peer"`). `mark_firmware_delivery` signature becomes `(device_id, status, error: Option<&str>)`; `record_delivery` in `ota.rs` passes `delivery_error.as_deref()` on the failure branches and `None` on the delivered branch so the column stays null on success. The `ota_success_rate` rule appends `Last error: {reason}.` to its detail string when it tips WARN/FAIL (picks the newest `failed` row's error, skips rows with NULL errors to stay backward-compatible), so admins see the failure category inline instead of cross-referencing the firmware history. Verified live via injected row: "5/8 of the last OTA uploads were delivered (62%). Last error: body: Broken pipe (os error 32)." Seeded at 2026-04-20 after a repro session where the kickoff's "relay-subnet binding" hypothesis for the 3 pre-v0.15.0 failures was falsified — tcpdump showed the ESP32 completing TCP handshakes cleanly; the actual previous failures were transient rtl8xxxu dongle flake that resolved after `ip route flush cache`. Going forward every `"failed"` row carries its *why*. 2 new unit tests (rule surfaces the newest error when not OK; stays silent on OK) — 43 → 45 total.
- [x] **Concurrent-OTA-safe outcome attribution (post-v0.14.0)** — `mark_firmware_delivery` previously targeted "most recent NULL `delivery_status` row for the device" via a subquery. Correct under the single-in-flight-OTA-per-device contract the UI ships today, but if two OTAs to the same device ever run concurrently outcome A could land on row B and vice-versa. Signature is now `mark_firmware_delivery(row_id: i64, status, error)` with a direct `WHERE id = ?1`; the three insert-and-serve paths (desktop `upload_firmware`, desktop GitHub OTA in `commands.rs`, REST `POST /api/github/ota` in `api.rs`) capture `store_firmware_record`'s returned rowid and thread it into `ota::serve_firmware(..., Some(row_id))`. `record_delivery` accepts `Option<i64>` and skips the write entirely on `None`, which is what `rollback_firmware` now passes — rollback reuses an existing firmware_history row and inserts no new one, so under the old code it was quietly flipping a random stale NULL row; now it's correctly a no-op. Live race proof against the running DB: two synthetic NULL rows (id=11 @ 16:50, id=12 @ 16:55) inserted, the new UPDATE targeted id=11 exactly, id=12 stayed NULL — under the old "most recent NULL" logic id=12 would have been the victim. 59 tests pass, no rule-count change (pure plumbing).
- [x] **OTA cancellation (v0.16.0)** — every running OTA transfer can now be aborted from the Upload Firmware card on the desktop OTA page (new "Cancel transfer" button appears while `status === "uploading"`) or from the web dashboard diagnostics `firmware_update` toast (inline Cancel button appears after the OTA is triggered). New `ota::OtaRegistry` (`HashMap<device_id, Arc<Mutex<bool>>>`, registered as a Tauri-managed state in `lib.rs`) replaces the `_stop_flag` each `serve_firmware` callsite was discarding; the worker registers its flag on start and removes it inside `finish()` on every exit path. `serve_firmware` rewritten into two cancel-responsive loops: a non-blocking `accept()` poll on the listener (~200 ms ticks) handles cancel-before-connect, and a 4 KB chunked write loop with a 500 ms `set_write_timeout` handles cancel-mid-transfer (the exact rtl8xxxu "Send-Q frozen" scenario from the 2026-04-20 hardware-test session). On cancel, the worker persists `delivery_status = "cancelled"` + `delivery_error = "Cancelled by user"` via the existing `record_delivery` path and emits the existing `ota_delivery_failed` event with `error: "cancelled"` — the desktop OTA page routes that into a new `status === "cancelled"` UI state (calm zinc banner, no "OTA update failed" alarm). New Tauri command `cancel_ota(device_id) -> bool` and admin-gated REST endpoint `POST /api/ota/cancel` (both return whether a registered OTA was found and flipped). `check_ota_success_rate` filter updated to exclude `"cancelled"` rows from the ratio denominator — user aborts don't count as delivery failures. Five new unit tests (4 registry + 1 cancelled-excluded) — 59 → 64 total.
- [x] **Library idle-warn after `begin()` timeout (v0.16.0)** — when `Trellis::begin(ssid, password)` returns false (WiFi timeout), the sketch's `trellis.loop()` call previously just hit the `if (!_webServer) return;` guard and went silent forever, giving a developer debugging a misconfigured sketch one `[Trellis] WiFi connection timeout` line and then dead serial. `Trellis::loop()` now emits `[Trellis] begin() has not succeeded — idle (check WiFi credentials)` every `IDLE_WARN_INTERVAL_MS` (30 s) on the null-`_webServer` path, backed by a new `_lastIdleWarn` field. Zero API change, library-patch only. Also cleared a stale BACKLOG entry that hypothesised a lingering null-deref past the 2026-04-08 guard (commit 7dc3074); that hypothesis was falsified on-device this session — flashing the current build produced zero panics across 60 s of idle watch. The observed crash from the 2026-04-21 session start was a stale flashed image (ELF SHA `5cc50a8f2`) pre-dating the guard, not a live bug. Verified on real ESP32 (ttyUSB0): 3 idle warnings in 80 s of serial capture, zero panics.
- [x] **mDNS resolution latency rule (v0.17.0)** — new `mdns_latency` diagnostics rule surfaces slow or unstable mDNS discovery by reading per-device `ServiceFound → ServiceResolved` elapsed time from the browse loop. Pure desktop-side feature, no library change. Capture: `mdns_browse_loop` keeps a local `HashMap<fullname, Instant>` of ServiceFound timestamps; on the matching `ServiceResolved` (`info.get_fullname()` as key), the elapsed millis is recorded via new `db.record_mdns_latency(device_id, latency_ms)` after `fetch_device_info` resolves the device_id. `ServiceRemoved` drops the pending entry so a later re-announce starts a fresh measurement; on `recv_timeout` a `retain(elapsed < 10s)` prune keeps the map bounded when Found fires without a subsequent Resolved (daemon restart, device vanishing mid-query). Cache-refresh resolves without a matching Found are silently skipped — we only record when we actually have both endpoints of the measurement. New `device_mdns_latency` table (`id`, `device_id`, `latency_ms`, `recorded_at`) with idx on `(device_id, recorded_at)`; `db.get_mdns_samples(device_id, hours)` returns the last 50 in the window newest-first. The rule reads samples over the 24h `WINDOW_HOURS`, computes median and max, and escalates: **FAIL** at median ≥500 ms or any sample ≥3 s (suggestion points at network congestion, packet loss check via `ping`, flaky WiFi dongle, mdns-sd daemon pressure), **WARN** at median ≥100 ms or any sample ≥1 s (monitor but don't alarm), **OK** otherwise, **INFO** until ≥3 samples accumulate (single lucky resolve isn't a reading). Finding renders generically via both desktop `DeviceDiagnostics.tsx` and web `renderDiagFinding`. Verified live against running binary at v0.16.0 post-rebuild: seeded 2 → INFO (below floor), 10× 15-30 ms → OK, +1 spike 1200 ms → WARN, median 150-240 ms → WARN, median 600-800 ms → FAIL, +1 spike 3200 ms → FAIL, cleared → INFO. Seven new unit tests (empty → INFO, sub-floor → INFO, clean-fast → OK, single 1 s spike → WARN, elevated median → WARN, slow median → FAIL, 3 s+ spike → FAIL) — 74 → 81 library tests.

- [x] **mDNS cadence capture-model rework (v0.18.0)** — `mdns_latency` rule reworked from per-announcement `ServiceFound → ServiceResolved` elapsed time to inter-Resolved cadence, closing the undercount documented in `project_mdns_latency_capture.md`: the v0.17.0 model only captured a sample at the *initial* announcement (TTL refreshes re-emit `ServiceResolved` without `ServiceFound`, and cross-subnet devices emit neither), so the rule sat at INFO indefinitely on healthy same-subnet steady state. New capture path in `discovery.rs::mdns_browse_loop` replaces `pending_found` / `PENDING_FOUND_TTL` with a `last_resolved_at: HashMap<fullname, Instant>` + `RESOLVED_DEBOUNCE = 5s` classifier (three outcomes — `DropDuplicate` = redundant interface re-fire within debounce, skipped entirely; `FirstSeen` = seed-only no sample; `Cadence(ms)` = record). The debounce collapses the 8-interface fan-out on a machine with docker/vethX bridges back to one event per announcement, and `RESOLVED_ENTRY_TTL = 30min` prunes stale entries so a device that vanishes without a `ServiceRemoved` doesn't record a pathological multi-hour "interval" on re-announcement. `ServiceRemoved` also clears the baseline. Schema: existing `device_mdns_latency` table grows a `sample_kind TEXT NOT NULL DEFAULT 'resolution'` column (ALTER TABLE migration for upgrades, default tags pre-existing rows correctly); new rows from the cadence path insert with `'cadence'`. Struct renamed `MdnsLatencySample → MdnsCadenceSample` (field `latency_ms → interval_ms`); DB methods renamed `record_mdns_latency → record_mdns_cadence` and `get_mdns_samples → get_mdns_cadence_samples` with `WHERE sample_kind = 'cadence'` filtering so stale legacy rows don't contaminate percentile math. Rule id stays `"mdns_latency"` so dashboards, alerts, and stored configs referencing it don't churn. Thresholds anchored to the expected TTL-driven refresh cadence (ESPmDNS default 120s, mdns-sd-rs re-queries at ~80% TTL): **FAIL** at p95 ≥600s (≥5× TTL — device likely dropping off or LAN path flaking), **WARN** at p95 ≥300s (≥2.5× TTL — occasional drops), **OK** below, **INFO** until ≥3 samples accumulate. The 0-sample INFO path gets distinct text calling out cross-subnet devices (`trellis-fccfb7c8` on 192.168.1.x reached from desktop on 192.168.2.x) so admins understand an empty reading is the *expected* shape for that topology, not a broken rule. Detail text reports p50/p95 in seconds (human-readable at 100s-scale intervals) with the expected TTL baseline inline. Verified live on trellis-fccfb7c8: rule starts at cross-subnet INFO, synthetic-inject 10 healthy samples → OK at p50=115s p95=135s, inject two 800-900s stretches → FAIL at p50=120s p95=900s with "dropping off" suggestion, cleanup → INFO restored. Capture-path real-traffic validation deferred — our lab ESP32 is cross-subnet and can't reach the desktop's mDNS browser by design; rule math is covered by 8 unit tests and end-to-end integration is proven via DB inject. Eight unit tests (0-sample cross-subnet text / below-floor / healthy OK / stretched p95 WARN / past-FAIL-line / small-N single-spike FAIL / ms→s unit conversion / rule-id stability) — 89 → 90 total.

- [x] **Energy coverage rule (v0.20.0)** — new `energy_coverage` diagnostics rule names whether the energy-tracking feature is wired up on this device. Pure visibility nudge — no failure mode, no escalation. **OK** when at least one switch capability has `nameplate_watts` set in `capability_meta` (detail: "Energy tracking configured for M of N switches"). **INFO** when zero switches are metered, with a suggestion pointing at Device Detail → Controls → ⚡ editor and naming what unlocks (Energy card with kWh/cost + per-switch power sensor in MQTT/Home Assistant). **INFO** "does not apply" when the device has zero switch capabilities (sensor-only nodes, etc.) — emitted with no nudge so the diagnostics card explains the absence rather than going silent. Rule is a pure function over `(meta: &[CapabilityMeta], capabilities: Option<&[Capability]>)`; `live_device.map(|d| d.capabilities.as_slice())` flows through `diagnose()` so an offline device degrades to a meta-only reading (loses the "M of N" denominator but still distinguishes configured-or-not). Six new unit tests (no meta + no caps → INFO fallback / caps present + no watts → INFO with switch-count framing / meta rows all NULL → INFO / one of two switches metered → OK with "1 of 2" / sensor-only device → INFO does-not-apply / rule_id stability) — 106 → 112 library tests. Verified live on trellis-fccfb7c8 (LED at 60W in capability_meta): clear watts → INFO with the ⚡ Controls suggestion, restore 60W → OK "1 of 1 switch", cleared again → INFO, restored → OK. Banks toward v0.20.0 alongside the three other post-v0.19.0 features (online-aware on-time intersection, web dashboard energy parity, MQTT/HA power sensor).

- [x] **Flash-wear rate rule (v0.18.0)** — new `flash_wear` diagnostics rule surfaces NVS write rates that would chew through the ESP32 flash partition prematurely, closing out the Diagnostics v5 roadmap (mdns_latency + power_supply_stability + flash_wear). Library side: `Trellis` class gains a RAM-only `uint32_t _nvsWrites` counter incremented inside both `#if defined(ESP32)` persist blocks in `TrellisWebServer::processCommand` (`setSwitch` putBool + `setSlider` putFloat). RAM-only by design — persisting the counter itself would double the flash wear we're trying to measure. Exposed as `system.nvs_writes` on both `/api/info` and the heartbeat WS frame (ESP32-only; Pico W omits the field since it has no NVS partition to wear). Counter resets to 0 on reboot; the rule handles that by summing only positive deltas between consecutive samples. Desktop side: `SystemInfo` (Rust + TS) gains optional `nvs_writes` field; `deviceStore.ts` heartbeat handler stores it as `_nvs_writes` via the existing `store_metric` pipeline, reusing the `metrics` table (zero migration). The rule reads samples over the 24h `WINDOW_HOURS`, sums positive deltas, and projects to a 24h rate based on sample span. Thresholds anchored to ESP32 NVS reality (~20 KB / 5 sectors × 100k cycles ÷ 2× wear-leveling amplification ≈ 250k lifetime writes): **FAIL** at ≥20000/24h (partition wears in weeks — suggestion points at tight automation loops firing setSwitch/setSlider without state checks), **WARN** at ≥5000/24h (months of life — investigate), **INFO** at ≥500/24h (elevated but years of life, typical for active automation), **OK** below. INFO with explanatory detail below the sample-span floor (need ≥2 samples spanning ≥1h). Finding renders generically via both desktop `DeviceDiagnostics.tsx` and web `renderDiagFinding`. Hardware-tested end-to-end against trellis-fccfb7c8 on ESP32: counter increments 1:1 with toggles (10 toggles → nvs_writes=10), desktop stores as _nvs_writes via heartbeat, synthetic injection forcing 33600/24h correctly fires FAIL (fleet `critical` went 3→4 on inject, 4→3 on cleanup — isolated the rule's contribution). Eight new unit tests (fewer-than-two / sub-1h span / OK-light / INFO-active / WARN-months / FAIL-pathological / reboot-reset handling / flat-counter) — 81 → 89 library tests.

- [x] **Power-supply stability rule (v0.17.0)** — new `power_supply_stability` diagnostics rule surfaces brownout clusters before they escalate into a device that "keeps rebooting" with no attribution. Library side: `TrellisTelemetry` captures `esp_reset_reason()` once at construction (stable across the session) and maps the IDF enum to a stable string (`poweron`, `external`, `software`, `panic`, `interrupt_watchdog`, `task_watchdog`, `watchdog`, `deepsleep`, `brownout`, `sdio`, `unknown`); emitted in the `system` block of both `/api/info` and the heartbeat WS frame as `reset_reason`. Non-ESP32 builds fall back to `"unknown"`. Desktop side: new `device_reset_history` table (`id`, `device_id`, `reset_reason`, `recorded_at`) with an idx on `(device_id, recorded_at)`. `discovery.rs` detects reboots by watching the in-memory `uptime_s` for a strict decrease between consecutive `/api/info` fetches — both the mDNS-browse and the cross-subnet health-check paths capture the prior uptime before assigning the new `SystemInfo` and call `db.record_reset` when old > new (the `old_uptime == 0` hydrated baseline is deliberately excluded so cold-starting the desktop doesn't mis-record a reboot per device). Pre-v0.17.0 firmwares that omit `reset_reason` get persisted as `"unknown"` so the rule still counts the reboot. The rule reads the last 50 resets within the 24h `WINDOW_HOURS` via new `db.get_resets`, classifies `brownout` as the direct PSU signal and `panic`/`*_watchdog` as secondary instability markers, and escalates: **FAIL** at ≥2 brownouts in 24h (with suggestion pointing at higher-current USB adapters, shorter cables, bulk decoupling), **WARN** at 1 brownout *or* ≥3 unexpected resets without a brownout, **OK** otherwise, **INFO** when no reboots have been observed. Reasons kept OFF the unexpected-reset list — `poweron`, `software`, `external`, `deepsleep`, `unknown` — are either expected user/OTA actions or ambiguous signals that would false-positive (the `"unknown"` case specifically keeps existing fleets silent the moment they upgrade the desktop). Finding renders generically in both the desktop `DeviceDiagnostics.tsx` and the web `renderDiagFinding` without any UI change. Library compiles for both ESP32 and Pico W (rp2040:rp2040:rpipicow). Seven new unit tests (no-reboots → INFO, clean reboots → OK, single brownout → WARN, two brownouts → FAIL, panic cluster → WARN, unknown-only → OK, helper coverage) — 67 → 74 library tests.

- [x] **Two-phase OTA apply tracking (v0.16.0)** — closes the "delivered means bytes left the desktop socket, not that the new firmware is actually running" gap that the v0.15.0 `ota_success_rate` rule exposed. Two new nullable columns on `firmware_history`: `delivery_ack_nonce` (32-hex capability token generated via `OsRng` per OTA — same primitive `auth.rs` uses for bearer tokens — stored at insert time) and `delivery_applied_at` (set by the device-side confirmation). The three desktop OTA start-sites (`commands.rs::start_ota`, `commands.rs::start_github_ota`, `api.rs::handle_github_ota`) generate a fresh nonce and thread it through `ota::serve_firmware`, which returns an `OtaServeHandle { fw_url, ack_url }` struct. `ack_url` is `http://<desktop-ip>:9090/api/ota/ack/<nonce>` — included in the `{"command":"ota","url":…,"ack_url":…}` WS message the device receives. `TrellisOTA::update(url, ackUrl, broadcaster)` (library) gains the optional `ackUrl` parameter; on successful `HTTP_UPDATE_OK`, it persists the URL to NVS (namespace `trellis_ota`, key `ack_url`) *before* the caller calls `ESP.restart()`. On the next boot, `Trellis::begin` (both `beginAutoConnect` and the credentials variant) calls `TrellisOTA::sendPendingAck(_firmwareVersion)`, which reads the stored URL, clears NVS immediately (so a transient network glitch can't trap the device in a boot-retry loop), and fires one `HTTPClient` POST with a 5 s timeout and a `{"version":…}` body. Server-side, `POST /api/ota/ack/<nonce>` is **pre-auth** and gated by the nonce itself — routed in `handle_connection` after the rate limiter (which gets any 400/404 recorded as an auth failure to disincentivize blind-guessing) but before `auth::check_auth`, since the device can't carry a bearer token. `mark_firmware_applied_by_nonce` classifies outcomes: `Applied` (200, newly stamped), `AlreadyApplied` (idempotent 200 on retries), `UnknownNonce` (404 — device drops pending ack), `DeliveryNotOk` (409 when the matched row is `cancelled`/`failed`). Rollbacks pass `None` through to both `store_firmware_record` and `serve_firmware` — no new row, no nonce, no ack command — so the legacy path is untouched. The `ota_success_rate` rule's numerator now counts `delivery_applied_at IS NOT NULL` (with a 5-minute grace window for just-rebooting devices, and rows without a nonce treated as "applied on delivery" to cover the rollback/pre-v0.16.0 shape); a new FAIL/WARN detail variant surfaces "N delivered but the device never confirmed the new firmware booted" to distinguish reboot failures from transport failures. React OtaManager gets an "Apply confirmed" chip on the success banner when the `ota_applied` Tauri event arrives for the selected device; the web dashboard's GH-OTA status upgrades to "Apply confirmed — the new firmware is running on the device" and toasts when the same event broadcasts over the `:9090` WS. Hardware-verified on ESP32 during the v0.16.0 release cut (2026-04-21): file-upload OTA of `BasicSwitch-v1.0.1.bin` → nonce `ce5bcfef` minted → device rebooted + rejoined WiFi → POST `/api/ota/ack/<nonce>` at 10:45:42 → `delivery_applied_at` stamped → `ota_success_rate` rule credited the applied count (1/9 confirmed). Earlier also verified via 11-scenario synthetic proof against the live server (Applied → DB stamp + idempotent re-POST → AlreadyApplied; Cancelled row → 409; Unknown nonce → 404; Malformed nonce → 400). 64 → 67 library tests (3 new: applied-count + grace window + rollback-no-nonce).

### OTA Updates
- [x] Drag & drop .bin firmware file
- [x] Upload firmware to selected device (ESP32, native file picker)
- [x] Progress bar during OTA (WebSocket events from device)
- [x] Success/failure notification
- [x] Firmware version tracking (history per device, stored in SQLite)
- [x] Firmware rollback (one-click re-flash previous firmware)
- [x] Firmware version comparison (current vs new)
- [x] **OTA from GitHub Releases (post-v0.4.7)** — point to any GitHub repo (`owner/repo` or full URL), app fetches releases via GitHub API, shows .bin firmware assets with tag/date/size, one-click download + flash via existing OTA pipeline. Per-device repo binding persisted in settings. Version comparison highlights the release matching the device's current firmware. REST API endpoints (`GET /api/github/releases`, `POST /api/github/ota`) enable the same flow from the web dashboard. Completes Tier 4 "Firmware OTA from GitHub".
- [x] **GitHub OTA polish (post-v0.4.8)** — download progress bar (chunked reads with per-2% events, shown in both desktop app and web dashboard via WS broadcast), user-friendly error messages (404/403/network mapped to plain-English explanations, web dashboard `api()` reads JSON error bodies), pre-release filtering toggle (hidden by default, checkbox when pre-releases exist, amber badge), asset name filter input (substring match, per-device persistence, hides releases with zero matching assets). All four surfaces: desktop OTA page + web dashboard detail panel.

### Settings & Preferences
- [x] Dark theme (default)
- [x] Scan interval configuration (persisted)
- [x] Device nicknames (inline edit, persisted in SQLite)
- [x] **Per-device notes (post-v0.20.0)** — free-form text field per device (wiring, install date, calibration, breaker number — anything worth remembering). New `notes TEXT NOT NULL DEFAULT ''` column on `devices` table with matching ALTER-TABLE migration for upgrades. Rust CRUD via `Database::set_device_notes`, exposed as `set_device_notes` Tauri command + admin-gated `PUT /api/devices/:id/notes` REST endpoint (body `{"notes": "…"}`). Desktop: new `DeviceNotes` React component renders as its own "Notes" section on the Device Detail page between Diagnostics and Uptime History — empty state is a dashed-border "+ Add notes" affordance, populated state shows the text in a pre-wrapped card with a pencil edit button, edit mode uses a resizable textarea with Save/Cancel. Embedded `:9090` web UI has full parity via `renderDeviceNotes`/`saveDeviceNotes` in `web_ui.html` following the existing `renderGithubRepoBinding` pattern (display/edit/empty states mirror the desktop surface). Viewer role: read-only on both surfaces — the "+ Add notes" affordance is suppressed and the Edit button is hidden (web UI also hardens the client-side `startEditNotes` / `saveDeviceNotes` against manual DOM calls), and the REST endpoint already enforces admin via `require_admin`. Notes are included in `get_saved_device` / `get_all_saved_devices` so the web UI's cached `savedDevices` list picks them up at page load; cross-surface edits settle on page refresh (matches how nickname/tags already work).
- [x] **Per-device install date (post-v0.20.0)** — optional yyyy-mm-dd set-once metadata per device recording when the unit was put into service. Complements nickname/tags/notes as part of the "device identity" cluster. New `install_date TEXT NOT NULL DEFAULT ''` column with matching ALTER-TABLE migration. Rust CRUD via `Database::set_device_install_date`, exposed as `set_device_install_date` Tauri command + admin-gated `PUT /api/devices/:id/install-date` REST endpoint (body `{"install_date":"2026-04-23"}` or `""` to clear). REST layer validates ISO format (`is_iso_date`) so direct curl writes can't poison the `<input type="date">`. Desktop: new `DeviceInstallDate` React component renders as its own "Install Date" section below Notes on the Device Detail page — empty state is a dashed "+ Set install date" affordance, populated state reads "Installed Apr 23, 2026" with a pencil edit button; edit mode uses a native `<input type="date">` with Save / Cancel / Clear. Embedded `:9090` web UI has full parity via `renderDeviceInstallDate`/`saveDeviceInstallDate` in `web_ui.html` mirroring the notes pattern. Viewer role read-only on both surfaces. Install date is included in `get_saved_device`/`get_all_saved_devices` and in config export/import (replayed at import alongside notes so re-imports don't silently lose it).
- [x] Window state persistence (size, position)
- [x] Minimize to system tray (close hides, tray restores, right-click quit)

### Get Started Wizard (Onboarding)
- [x] **First-run redirect** — new users auto-redirected to `/get-started` on first launch (persisted via `onboarding_completed` setting)
- [x] **4-step guided wizard**: Welcome/Prerequisites → Pick Template → Configure & Flash → Device Appeared
- [x] **5 bundled starter templates**: Blink (LED toggle), Sensor Monitor (analog + text), Smart Relay (switch + timer), Weather Station (temp/humidity/pressure), Greenhouse Controller (soil moisture + pump + grow light)
- [x] **Prerequisite checks**: arduino-cli detection, board core + library dependency check, one-click install for missing deps
- [x] **Template-to-flash pipeline**: select template → customize device name/board/capabilities → compile & flash via USB in one click
- [x] **Device discovery confirmation**: step 4 watches mDNS for the new device, shows success with device info when found, or WiFi provisioning instructions if waiting
- [x] **Dashboard empty state integration**: "Get Started" button alongside "Add by IP" when no devices found
- [x] **Sidebar entry**: always accessible for re-running the wizard
- [x] **Shared sketch generator**: both wizard and New Device page route through the backend `generate_sketch_command` (single source of truth — see Sketch Generator section)

### Quick Flash (arduino-cli integration)
- [x] Detect arduino-cli installation (version check)
- [x] Compile generated sketch (ESP32 + Pico W FQBN mapping)
- [x] Flash via USB (serial port selector + upload)
- [x] Build output panel (color-coded success/errors)
- [x] Auto-reset on capability/board changes

### Sketch Generator (v0.28.0)
- [x] **Sketch generator core (v0.28.0)** — config-driven `.ino` template engine. New `sketch_gen` module produces a complete Arduino sketch from a `SketchSpec { device_name, firmware_version?, board?, capabilities: [{kind, id, label, ...}] }` — kinds are `switch` / `slider` / `color` / `sensor` / `text`. Output includes the canonical `#include <Trellis.h>`, the `Trellis trellis(...)` constructor, an `onCommand` handler with typed stub branches per slider (`value.as<float>()`) and color (hex parse → R/G/B), per-sensor `analogRead()` skeletons when GPIO is provided, and a `setup()` body emitting one `addX` call per capability followed by `trellis.beginAutoConnect()` (captive-AP credential provisioning). Board-aware reset call (`ESP.restart()` on ESP32, `rp2040.reboot()` on Pico W). Strict input validation: identifier `[a-zA-Z0-9_]+`, label/device-name reject newlines + quotes + backslashes, GPIO range 0-48, slider min<max with finite values, ≤16 capabilities, unique cap IDs, semver firmware_version. New Tauri command `generate_sketch_command(spec) -> Result<String, String>` + admin-gated REST `POST /api/sketch/generate` returning `{"sketch": "..."}` or `400` with descriptive error. Hardware-verified end-to-end: representative all-kinds spec generated → `arduino-cli compile --fqbn esp32:esp32:esp32` clean (91% flash, 16% RAM) AND `--fqbn rp2040:rp2040:rpipicow` clean (22% flash, 28% RAM) against the published Trellis 0.27.0 library. 21 lib tests covering all kinds, board variants, validation, and hotspot slugification.
- [x] **Sketch generator wizard (v0.28.0)** — single source of truth: the desktop `New Device` page (route `/new-device`) and the embedded `:9090` `Sketch` tab now both call the backend `generate_sketch_command` / `POST /api/sketch/generate` directly. The duplicated frontend `lib/sketchGenerator.ts` template was retired in favor of a thin async wrapper that maps the UI's capability shape to the backend `SketchSpec`. Desktop wizard re-renders the preview reactively on every input change, surfaces backend validation errors inline (so users see the reason the sketch can't be generated mid-edit), and re-generates fresh on Compile / Compile & Flash so what gets compiled is always what's previewed. `:9090` wizard mirrors the same shape: device name + board + optional firmware version, capability builder with one-click add buttons (switch/slider/color/sensor/text), inline per-row id/label/GPIO/unit/min/max fields, generated source preview pane, Copy + Download .ino actions. Admin-gated (viewers see a "requires admin token" notice instead of the form). The Get Started onboarding wizard's compile-and-flash path swaps to the same backend call so onboarding and the standalone generator emit byte-identical sketches. Hardware-verified end-to-end with `arduino-cli compile` against an all-kinds smoke fixture (5 caps incl. `text` and a sensor with GPIO) on both ESP32 and Pico W FQBN.
- [x] **Library manifest auto-population (v0.28.0)** — closes the version-skew risk where the generator could outpace the library API. New repo-root `lib_manifest.json` declares `{library, version, capabilities, boards}` and is the single source of truth for what the bundled Trellis library supports. The desktop binary embeds it via `include_str!` at build time so the manifest always matches `library.properties`. New `lib_manifest` Rust module exposes `current()` (parsed `LibManifest` cached via `OnceLock`) and `validate_capability_kinds(spec)` (defense-in-depth: rejects specs carrying capability kinds the library doesn't declare). New Tauri command `get_sketch_lib_info_command` + admin-gated `GET /api/sketch/lib-info` returning the manifest as JSON. Both wizards (desktop `FirmwareGenerator` + `:9090` Sketch tab) fetch the manifest on mount, render a "Library: Trellis X.Y.Z" badge, and filter capability buttons + board dropdown options to manifest entries — so a future library that drops or renames an `addX` method automatically removes that button from the wizard. Hardware-verified: `arduino-cli compile` clean on ESP32 (91% flash) + Pico W (22%) for the all-kinds fixture. CDP smoke verified the badge renders, all 5 capability buttons appear (one per manifest kind), the board dropdown matches manifest entries, and an unknown kind posted directly to `POST /api/sketch/generate` is rejected. Release-cut bump list expanded 6 → 7 files (`lib_manifest.json` joins the version-bump set; missing it would lag the wizard's library badge). 4 new lib tests (235 → 239) including a regression guard that asserts the manifest accepts every `CapabilitySpec` variant — adding a new variant without updating the manifest fails the test.

### App Shell
- [x] Sidebar navigation
- [x] Header with connection status
- [x] About dialog with version

## Microcontroller Library

### Core
- [x] `Trellis` class — main entry point
- [x] `begin(ssid, password)` — WiFi connect + start services
- [x] `loop()` — process events
- [x] Capability registry (add at setup time)

### Capability Types
- [x] `addSwitch(id, label, gpio)` — digital output
- [x] `addSensor(id, label, unit)` — read-only value
- [x] `setSensor(id, value)` — update sensor reading
- [x] `addSlider(id, label, min, max, gpio)` — PWM output
- [x] `setSlider(id, value)` — update slider value + apply PWM
- [x] `addColor(id, label)` — RGB value
- [x] `addText(id, label)` — text display/input
- [x] `onCommand(callback)` — custom command handler

### NVS Persistence (v0.4.7, ESP32 only)
- [x] Switch values persist across reboots — GPIO state applied on boot before first client connects
- [x] Slider values persist across reboots — PWM duty applied on boot before first client connects
- [x] Shared `trellis_cap` NVS namespace, keyed by capability ID (max 15 chars)
- [x] Pico W degrades gracefully — values start at defaults, no persistence

### Networking
- [x] WiFi connection with timeout
- [x] mDNS service advertisement (`_trellis._tcp`)
- [x] HTTP server: `GET /api/info` — capability declaration
- [x] WebSocket server — real-time commands & telemetry

### Embedded Web Dashboard (v0.3.0)
- [x] On-device control panel served at `GET /` from PROGMEM (no RAM cost)
- [x] Self-contained: vanilla HTML/CSS/JS, no build step, no external CDN
- [x] Renders all 5 capability types (switch, slider, sensor, color, text)
- [x] Live updates via WebSocket — auto-reconnect on broker drop
- [x] System telemetry tiles (signal, free heap, uptime)
- [x] Mobile-first responsive layout, dark theme, inline SVG favicon
- [x] Default-on, opt-out via `trellis.enableWebUI(false)` (toggleable at runtime)
- [x] Saves ~13 KB of flash when disabled
- [x] Verified end-to-end on real ESP32 hardware

### Embedded Web Dashboard — Polish (v0.3.1)
- [x] Live log viewer panel — collapsible, severity-coloured, ring-buffered (200 lines), pause/clear, unread badge
- [x] OTA progress overlay — start tick, failure tick, reboot detection, auto-reload on success
- [x] Add-to-home-screen hint — mobile-only, iOS/Android-aware wording, dismiss persisted in localStorage
- [x] Apple touch icon (180×180) + theme-color + mobile-web-app-capable meta tags for proper PWA installation

### Embedded Web Dashboard — Cache Invalidation (v0.3.3)
- [x] **ETag-based conditional GET tied to library version + content hash** (`"<TRELLIS_VERSION>-<sha256-prefix>"`). Replaces the previous `Cache-Control: max-age=300` which caused browsers to serve stale UI for up to 5 minutes after a firmware push. The version part is for human inspection; the hash part is the actual cache key, so HTML changes invalidate even if a release forgets to bump the version macro. `scripts/build_web_ui_header.py` now emits `TRELLIS_WEB_UI_HTML_HASH` alongside the PROGMEM byte array. `_http->collectHeaders()` registers `If-None-Match` so the conditional GET path can fire. Verified end-to-end on real ESP32: 200 + ETag on first GET, 304 on matching `If-None-Match`, 200 on mismatch.

### OTA
- [x] HTTP OTA update handler (ESP32)
- [x] OTA progress reporting via WebSocket
- [x] Auto-reboot after successful update
- [x] **Real-time OTA progress + delivery confirmation (post-v0.4.4)** — `httpUpdate.onProgress` streams real progress percentages (every 5%) over WebSocket during firmware download. After successful write, device sends explicit `ota_delivered` event before rebooting. Desktop and embedded dashboards show live progress bar and a "Firmware received" confirmation tick. Broadcaster callback decouples OTA from WebSocket library.

### Telemetry
- [x] RSSI reporting
- [x] Free heap reporting
- [x] Uptime reporting
- [x] Chip model reporting
- [x] Firmware version reporting

### Platform Support
- [x] ESP32 (all variants) — compiled + tested on hardware
- [x] Raspberry Pi Pico W — compiled
- [x] Raspberry Pi Pico 2 W — compiled
- [x] Platform abstraction layer (WiFi, mDNS, OTA)

## Protocol

- [x] `GET /api/info` — JSON capability declaration
- [x] WebSocket — bidirectional messages
- [x] Command: `{"command": "set", "id": "...", "value": ...}`
- [x] Event: `{"event": "update", "id": "...", "value": ...}`
- [x] OTA command: `{"command": "ota", "url": "..."}`
- [x] OTA progress event: `{"event": "ota_progress", "percent": N}`
- [x] Heartbeat: `{"event": "heartbeat"}` (periodic, every 10s)
- [x] Log event: `{"event": "log", "severity": "...", "message": "..."}`

### Push Notifications
- [x] ntfy.sh integration (send alerts to phone, no app account needed)
- [x] Push on alert trigger (sensor threshold exceeded)
- [x] Push on device offline
- [x] Settings UI for ntfy topic configuration
- [x] Test notification button

### Remote Access (v0.4.0)
- [x] **Settings → Remote Access panel** with two transport cards: Cloudflare Tunnel (recommended) and Tailscale Funnel (no-domain alternative). Each card has a step-by-step setup recipe with inline links to upstream installers. No third-party binaries bundled — the user installs `cloudflared` / `tailscale` themselves.
- [x] **Token-aware embedded web UI.** `web_ui.html` reads `localStorage.trellis_api_token` on every fetch and adds `Authorization: Bearer trls_…` when present. On loopback (no token needed) the dashboard works exactly as before. Through a tunnel, the first `/api/*` 401 pops an inline modal asking for a token; the user pastes it once, the page reloads, and every subsequent request carries the header.
- [x] **`GET /` always allowed** by the auth gate via a pre-auth special case in `api.rs::handle_connection`. The HTML itself is harmless static content; every dynamic surface (`/api/*`) still goes through the v0.3.4 token gate.
- [x] **Reachability probe.** New `probe_remote_url` Tauri command + Settings widget that runs a single `GET /api/devices` from the desktop machine through the user's tunnel and back. Classifies the result into `success`, `auth_failed`, `not_trellis`, `tunnel_down`, `network_error`, `timeout`, or `unexpected` and surfaces a human-friendly explanation. URL is persisted between probes; the token is held in component memory only.
- [x] **Safety check** on the Settings panel: amber warning card if zero API tokens are minted ("the tunnel will be reachable but unusable").
- [x] User guide §16 walks through both transports + the reachability probe, including the "mint a token first" prerequisite and a one-paragraph "why not ngrok" note.

### Per-Device Dashboard Proxy
- [x] **Reverse proxy at `/proxy/{device-id}/`** on :9090 — forwards HTTP requests to the device's embedded :8080 web server. Passes through the existing Bearer token auth gate so remote users (via Cloudflare Tunnel / Tailscale Funnel) can reach individual device dashboards without direct LAN access.
- [x] **WebSocket bridge at `/proxy/{device-id}/ws`** — detects `Upgrade: websocket` and bridges raw TCP to the device's WS port (:8081). Two-thread copy loop, long-lived, auto-closes when either side drops.
- [x] **HTML rewriting** — proxied root HTML is rewritten so `fetch("/api/info")` becomes relative (resolves through proxy) and the WebSocket URL routes through `/proxy/{id}/ws` instead of `host:port+1`. Protocol-aware (`ws://` or `wss://`) for tunnel compatibility.
- [x] **"Device Dashboard" link** in the desktop app device detail page (online devices only) and in the :9090 web dashboard device cards.

### REST API
- [x] HTTP API server on port 9090 (runs alongside desktop app)
- [x] Full device CRUD (list, get, delete, send command, set nickname/group)
- [x] Metrics, logs, alerts, firmware history endpoints
- [x] Groups, schedules, rules, webhooks CRUD
- [x] Settings read/write endpoints
- [x] CORS support for cross-origin access
- [x] CSV metrics export endpoint
- [x] **Bearer token authentication (v0.3.4)** — every non-loopback request must include `Authorization: Bearer trls_…`. Tokens minted in `Settings → API Tokens`, stored as SHA-256 digests, surfaced in plaintext exactly once at creation. Loopback bypass on by default so the desktop app and local CLI work with zero setup; opt-in `require_auth_localhost` setting for defense in depth on shared machines. Friendly HTML 401 page for browser users instead of bare JSON. New `/api/tokens` CRUD endpoints + `auth.rs` module with 13 unit tests covering token shape, hash stability, scheme parsing, loopback detection, expiry, and rate limiting. Closes the LAN-exposure surface that v0.3.3 only partially addressed.
- [x] **Token expiry / TTL (post-v0.4.3)** — optional `expires_at` on API tokens. TTL options: 1h, 24h, 7d, 30d, 90d, or never (default, backward-compatible). Auth gate rejects expired tokens with a distinct error message. Settings UI has a TTL dropdown in the create form and an Expires column with color-coded status (red=expired, amber=<24h). REST API `POST /api/tokens` accepts optional `ttl` field.
- [x] **Rate limiting + failed-auth backoff (post-v0.4.3)** — per-IP in-memory rate limiter with exponential backoff. After 3 grace failures, requests are rejected with 429 (1s → 2s → 4s → ... capped at 60s). Auto-resets after 15 minutes of silence or on successful auth. Loopback exempt. 5 unit tests.
- [x] **Role-based access control (post-v0.4.4)** — each API token carries a role: `admin` (full access) or `viewer` (read-only). Viewers can read devices, metrics, status, schedules, rules, webhooks, and token metadata but cannot send commands, trigger OTA, manage tokens, change settings, or access the device proxy. Existing tokens default to admin (backward-compatible). `Role` enum in `auth.rs`, `require_admin()` guard on 17 mutating endpoints + proxy. Settings UI has a role dropdown in the create form, role column in the token table, and role badge in the created-token modal. Web dashboard at `:9090` calls `GET /api/auth/whoami` on load to detect viewer tokens: disables toggles/sliders/color pickers/text inputs, hides group/ntfy write controls, and shows a "Read-only" badge in the header. 14 unit tests (including role parsing).

### Web UI Dashboard
- [x] Responsive web dashboard (phone + desktop) at localhost:9090
- [x] Device cards with live status, grouped view
- [x] Interactive controls (switch, slider, sensor, color, text)
- [x] Automation overview (schedules, rules, webhooks)
- [x] Settings management (ntfy, groups)
- [x] Auto-refresh (5s polling, fallback only)
- [x] Zero external dependencies (single embedded HTML file)
- [x] **WebSocket push (post-v0.4.4)** — persistent `/ws` connection replaces polling with instant device event delivery. Device state changes, heartbeats, logs, and discovery events (online/offline) arrive in real time. `WsBroadcaster` fan-out in Rust feeds all connected browser clients. Query-param token auth (`/ws?token=trls_...`) for remote access since browser WebSocket API can't set custom headers. Loopback bypass applies. Polling auto-resumes as fallback on WS disconnect. Green/gray connection indicator dot in header.
- [x] **PWA support (post-v0.4.4)** — web app manifest (`/manifest.json`) with standalone display mode, themed SVG icons, and dark background. Service worker (`/sw.js`) caches the HTML shell for offline display with network-first strategy. Mobile install prompt banner for "Add to Home Screen". Both routes served pre-auth (same as `GET /`).
- [x] **Notification preferences (post-v0.4.4)** — "Browser Notifications" settings section with three toggles: device offline (default on), device online (default off), and error logs (default off). Persisted to `localStorage`. Permission status label (Allowed/Blocked/Not yet requested) with color coding. Permission requested on first toggle-on. Viewers see toggles but cannot change them. Log error events from WS push now handled and can trigger notifications.
- [x] **Per-device notification filtering (post-v0.4.4)** — "Per-Device Overrides" section below global toggles. Each device gets three buttons (Offline/Online/Errors) that cycle through inherit (gray dashed border) → on (green) → off (red). Overrides stored in `localStorage` (`trellis_notif_device_prefs`), checked via `shouldNotify(key, deviceId)` before firing browser notifications. Absent overrides follow the global setting. Viewers see buttons but cannot change them.
- [x] **Device detail panel (post-v0.4.4)** — click "Details" on any device card to open a right-side slide-out panel showing: device info (status, ID, IP, firmware), system metrics (RSSI, free heap, uptime, chip) in a 2-column stat grid, interactive controls (same as card expansion), link to per-device embedded dashboard, and 20 most recent log entries with severity coloring. Closes on overlay click, close button, or Escape key.
- [x] **Device detail panel polish (v0.4.6)** — firmware history timeline (up to 5 past uploads with version, date, file size), inline SVG sparklines for RSSI and free heap (last 1h of stored metrics, 120×24px), and mobile-responsive layout (full-width panel, single-column stat grid, tighter spacing at ≤640px). All async fetches run in parallel.
- [x] **Notification sound (v0.4.6)** — optional audio chime when a browser notification fires. Two-tone sine wave (880Hz → 1175Hz, 0.4s) generated via Web Audio API — no external audio file. Toggle in Settings → Browser Notifications, default off. Respects viewer/unsupported disabled states.
- [x] **Interactive metric charts (v0.4.6)** — full SVG time-series charts in the device detail panel for WiFi Signal (amber), Free Heap (blue), and all sensor capabilities (green). Time range picker (1h / 6h / 24h / 7d), hover tooltips with crosshair + value + timestamp, touch support for mobile, auto-scaled Y-axis with grid lines, smart X-axis labels (HH:mm for ≤24h, Mon D HH:mm for 7d). Bucket downsampling caps at 200 points for smooth rendering with large datasets. Zero external dependencies.
- [x] **Metrics tab (v0.4.6)** — top-level monitoring overview showing all devices with 2-column chart grids, global time range picker (1h / 6h / 24h / 7d), online/offline status dots, and "Details" links to the slide-out panel. Reuses the interactive SVG chart renderer. Mobile-responsive (single column at ≤640px).
- [x] **Chart event annotations (v0.4.6)** — point-in-time event markers overlaid on every metric chart: OTA firmware uploads (blue), online/offline state transitions (green/red), device-reported errors and warnings (amber). Markers render as color-coded vertical dashed lines with circular hover targets; SVG `<title>` provides a native browser tooltip with the kind, label, and exact timestamp. A legend row appears below the chart listing only the kinds present in the current window. State transitions are persisted from `discovery.rs` into `device_logs` with `severity="state"` so they survive across restarts. New `GET /api/devices/{id}/annotations?hours=N` endpoint unions `firmware_history` + `device_logs` (state/error/warn), sorted oldest-first, capped at 200 per request. Fetched once per device in parallel with metrics and reused across all charts in the device detail panel and Metrics tab.
- [x] **Annotation click-through (v0.4.6)** — clicking any event marker on a metric chart opens the device detail panel and scrolls to + flash-highlights the underlying log row (state/error/warn) or firmware history row (ota). Each marker carries its kind, timestamp, and label as data-* attributes plus a 6px transparent hit target above the 3px visible circle for tap-friendly mobile use. Detail panel logs section now fetches up to 200 entries (was 20) and renders the full firmware history (was first 5). For noisy devices where info logs displace older annotation rows from the recent-200 window, a fallback fetch hits `GET /api/devices/{id}/logs?limit=200&severity=state,error,warn` and re-renders the section so the matching row is guaranteed to be present. New `severity` query param on `/logs` is additive — existing call sites and the Tauri `get_device_logs` command keep their unchanged signatures via a new `Database::get_logs_filtered` method.
- [x] **Recent Logs severity filter chips (v0.4.6)** — device detail panel's Recent Logs section now has a chip row above the list: **All / Events / State / Error / Warn / Info / Debug**. Chips reuse the `.chart-range-btn` style for visual consistency with the chart range picker and inherit its mobile tuning. 'Events' maps to `state,error,warn` (the same set that backs chart annotations, matching the Metrics tab legend). Clicking a chip re-fetches `/logs?limit=200&severity=...` and re-renders the list in place; clicking 'All' restores the unfiltered view. Shared `renderLogsList` helper dedupes the rendering logic between `openDeviceDetail` and `setLogFilter`. Stale-fetch guard via `currentLogDeviceId` check before and after the `await` prevents a mid-switch race from writing old results into the new panel. The annotation click-through fallback now calls `setLogFilter('events')` instead of doing a hidden inline re-render — the user sees the active chip update, making the filter state explicit and reversible.
- [x] **Uptime timeline (v0.4.6)** — device detail panel has a new **Uptime History** section between System metrics and Controls that renders a horizontal ribbon of online/offline segments over the current chart time range. Green = online, red = offline, gray = leading "unknown" segment before the first recorded transition (transitions only fire on state change, so the prior state is inferred but left gray because we can't prove the device was being tracked before window-start). The last segment always extends to "now" using the most recent transition's kind. Segments derive client-side from the same `/api/devices/{id}/annotations?hours=N` set already fetched for chart markers — no new backend work and no duplicate fetch. Native SVG `<title>` tooltips show `Online/Offline for Xh Ym (start → end)`; clicking a colored segment activates the **State** filter chip and flash-highlights the matching log row in Recent Logs (re-uses the annotation click-through pattern). Strip aligns horizontally with the chart data area (`pad.left=42, pad.right=10`) so visual scrubbing matches the metric charts below it. Empty-window case renders a single gray strip labeled "No state transitions in this window". Re-renders automatically on chart range change because `loadDetailCharts` calls `renderUptimeTimeline` after each fetch. A one-line summary above the ribbon rolls the segments into quantitative stats — `NN.N% online · Xd Yh tracked · N transitions` — where the denominator excludes the leading inferred "unknown" segment so the percentage reflects only the span we actually observed. Empty windows fall through to an italicized "No tracked uptime in this window".

## Scenes

- [x] Multi-action scene creation (set multiple devices at once)
- [x] One-click scene execution (sequential command dispatch)
- [x] ~~Scene persistence (localStorage)~~ Replaced by SQLite backend below
- [x] Device/capability/value selector per action
- [x] **Backend-backed scenes (post-v0.7.0)** — scenes persisted in SQLite (`scenes` + `scene_actions` tables) instead of localStorage. Full CRUD via Tauri commands (`create_scene`, `get_scenes`, `delete_scene`, `run_scene`) and REST API (`GET/POST /api/scenes`, `DELETE /api/scenes/{id}`, `POST /api/scenes/{id}/run`). Scene execution moved to backend (ConnectionManager sends commands to each device) so both desktop app and web dashboard can trigger scenes. New "Scenes" tab in `:9090` web dashboard with Run button per scene. Admin-only for create/delete/run.
- [x] **Scene editing (post-v0.8.0)** — edit existing scenes (rename + add/remove/reorder actions). `update_scene` Tauri command and `PUT /api/scenes/{id}` REST endpoint replace all actions atomically. Edit button on scene cards in both desktop app and `:9090` web dashboard opens inline form pre-filled with current name and actions. Offline devices preserved in edit dropdowns with "(offline)" label so actions aren't silently lost. Admin-only.
- [x] **Web dashboard scene CRUD (post-v0.8.0)** — full scene management in `:9090` web dashboard: "New Scene" button opens inline create form with device/capability/value selectors and action rows, Delete button on scene cards with confirmation. Previously only the desktop app could create/delete scenes. Admin-only (hidden for viewers).

## MQTT Bridge

- [x] In-app MQTT client (rumqttc), worker-thread design
- [x] Settings UI: broker host/port/username/password, base topic, HA discovery prefix, enable + test connection
- [x] Tauri commands: get_mqtt_config, set_mqtt_config, clear_mqtt_password, get_mqtt_status, test_mqtt_connection
- [x] REST API: GET/PUT /api/settings/mqtt, POST /api/mqtt/clear-password, GET /api/mqtt/status
- [x] Last-will availability (`<base_topic>/bridge/availability` retained)
- [x] Bidirectional state sync (Trellis → MQTT and MQTT → device commands)
- [x] Home Assistant MQTT discovery — auto-creates entities for switch/slider/sensor/color/text
- [x] **HA binary_sensor opt-in for sensor capabilities (v0.27.0)** — sensor caps with the `binary_sensor` flag set on `capability_meta` advertise to HA under the `binary_sensor` component instead of `sensor`, with `payload_on/off="true"/"false"` and an optional `device_class` (motion, door, occupancy, smoke, gas, moisture, sound, vibration, tamper, safety, problem, presence, window, opening, connectivity, or none). Two new columns: `binary_sensor INTEGER NOT NULL DEFAULT 0` + `binary_sensor_device_class TEXT`. New Tauri command `set_capability_binary_sensor` + REST `PUT .../capabilities/:cap/meta` accepts `{binary_sensor, binary_sensor_device_class}`. Toggling between sensor and binary_sensor retracts the alternate-component config (empty retained payload) so HA only sees one entity per cap at any moment. Desktop adds a `CapabilityBinarySensor` inline editor under each sensor card; `:9090` mirrors via `renderCapBinSensor`/`saveCapBinSensor`. Energy-tracking fields (`nameplate_watts`/`linear_power`) on the same row are independent. Hardware-verified live on trellis-fccfb7c8 via `mosquitto_sub`: toggle-on with `device_class=motion` published a clean binary_sensor config with the right payload_on/off; toggle-off republished the regular sensor config with `unit: "C"` and retracted the binary_sensor topic; toggle-on again with `device_class=door` retracted the sensor topic and published binary_sensor with door class.
- [x] **HA cover routing for slider capabilities (v0.27.0)** — slider caps with the `cover_position` flag set on `capability_meta` advertise to HA under the `cover` component instead of `number`. The discovery payload uses `position_topic = state_topic`, `set_position_topic = command_topic`, `position_open` taken from the slider's live max (fallback 100), `position_closed: 0`, and `optimistic: false` so HA waits for state echoes. New column `cover_position INTEGER NOT NULL DEFAULT 0` on `capability_meta`; new Tauri command `set_capability_cover` + REST `PUT .../capabilities/:cap/meta` accepts `{cover_position}`. Toggling between number and cover retracts the alternate-component config via the same generic `remove_alt_component_entity` helper from binary_sensor opt-in, so HA only ever sees one entity per cap. Desktop adds a `CapabilityCover` inline toggle under each slider card; `:9090` mirrors via `renderCapCover`/`saveCapCover`. Energy fields (`nameplate_watts`/`linear_power`/`slider_max`) and cover routing are independent. Hardware-verified live on trellis-fccfb7c8 via `mosquitto_sub`: toggle-on retracted `homeassistant/number/.../config` and published a clean cover config with `position_open: 100`; HA-style position write to `.../set` with payload `25` echoed via `.../state :: 25`; toggle-off retracted the cover config and republished the number config with `mode: slider`, max=100, min=0.
- [x] **HA `light` brightness+color linkage (v0.27.0)** — combine an existing color cap and a brightness slider on the same device into one HA `light` entity carrying both rgb and brightness channels. New column `brightness_for_cap_id TEXT` on `capability_meta`, set on the SLIDER row pointing at the linked color cap. The bridge promotes the color cap's discovery to a `light` with `schema: "json"`, `supported_color_modes: ["rgb"]`, `brightness: true`, and `brightness_scale` taken from the slider's live max; routes the light's state/command topics to a synthetic `_light` namespace (`<base>/<device>/<color_cap>/_light/{state,set}`); and skips publishing the slider's separate `number`/`cover` entity (retracted via the same `remove_alt_component_entity` helper). The synthetic state topic carries merged JSON `{state, color:{r,g,b}, color_mode:"rgb", brightness}` rebuilt from a per-link `(color_value, brightness_value)` cache whenever either side fires `publish_state`. Inbound HA-style writes to `_light/set` with `{state, color:{r,g,b}, brightness}` are split into separate WS commands fan-out: color → color cap (with `{r,g,b}` ↔ `#RRGGBB` translation since firmware speaks hex), brightness → slider cap. New Tauri command `set_capability_brightness_link(device, slider_cap, color_cap_opt)` + REST `PUT .../capabilities/:cap/meta` accepts `{brightness_for_cap_id}` (validates the target color cap exists on the same device). Desktop adds a `CapabilityBrightnessLink` dropdown under each color cap (lists same-device sliders); subordinate sliders show a "Brightness for <color cap>" readout in place of their standalone HA toggles. `:9090` mirrors via `renderCapBrightLink` / `renderCapBrightLinked` / `saveCapBrightLink`. Hardware-verified live on trellis-fccfb7c8 via `mosquitto_sub` after flashing TestDevice.ino with `addColor("rgb", "RGB Light")`: link → light config gained `brightness:true` + `brightness_scale:100` + `_light` topics, slider's `number` entity retracted; HA-style write `{state:ON, color:{r:0,g:255,b:128}, brightness:75}` → ESP32 echoed `rgb/state :: #00FF80` and `brightness/state :: 75`, merged `_light/state` reflected both as `{brightness:75, color:{b:128,g:255,r:0}, color_mode:rgb, state:ON}`; unlink → synthetic `_light/state` retracted, slider's `number` entity republished, light reverted to plain rgb.
- [x] HA diagnostic sensors per device — Signal strength (dBm), Free heap (B), Uptime (s, total_increasing)
- [x] Instant discovery on bridge enable (no 30s wait)
- [x] Republish discovery on broker reconnect (handles broker restart)
- [x] Heartbeat → MQTT mirroring (device telemetry visible in HA without Trellis desktop app open)
- [x] Multi-segment base topics (e.g. `home/iot/trellis`) supported via prefix-strip
- [x] **TLS/encrypted broker connection (v0.3.3)** — `tls_enabled` + optional `tls_ca_cert_path` (PEM, custom CA or system trust roots), wired into both `start()` and `test_connection()`. Settings UI has a CA file picker and auto-bumps port to 8883 when TLS is enabled. Verified end-to-end on broker.emqx.io / broker.hivemq.com (system trust roots) and a local Mosquitto + self-signed CA pair.
- [x] **Skip certificate verification (post-v0.4.4)** — `tls_skip_verify` boolean disables TLS cert validation when enabled. Connection is still encrypted but broker identity is not checked — equivalent of `curl -k`. Implemented via rustls `dangerous()` API with a `NoVerifier` that accepts any cert. Settings UI shows checkbox inside the TLS section with an amber security warning when active. Tested live against broker.emqx.io:8883.
- [x] **Password redacted in GET endpoints + sensitive-key blocklist (v0.3.3)** — `MqttConfigPublic` strips `password` from the wire shape with a `has_password` flag for the UI; generic `/api/settings/<key>` returns 403 for `mqtt_config`. Stops the LAN-exposed REST API on `:9090` from leaking the broker password to anyone on the same network.
- [x] **Encrypted password at rest with age (v0.3.3)** — `secret_store.rs` wraps an x25519 identity in the OS keyring (with 0600 file fallback). Wire format `enc:v1:<base64>` for stored passwords. Lazy migration of legacy plaintext blobs on first launch.
- [x] **Empty-password-preserves on save + explicit Clear (v0.3.3)** — `merge_preserving_password()` so the form load+save round-trip doesn't blank the stored password; `clear_mqtt_password` Tauri/REST endpoint and a Clear button in the Settings UI for the explicit-clear UX.

## Sinric Pro Bridge (v0.6.0)

- [x] WebSocket bridge to `wss://ws.sinric.pro` with HMAC-SHA256 signing, worker-thread design (mirrors MQTT bridge architecture)
- [x] Settings UI: API key, secret (encrypted at rest), device mappings with capability selector, test connection
- [x] Tauri commands: get_sinric_config, set_sinric_config, clear_sinric_secret, get_sinric_status, test_sinric_connection
- [x] REST API: GET/PUT /api/settings/sinric, POST /api/sinric/clear-secret, GET /api/sinric/status
- [x] Bidirectional switch mapping (setPowerState)
- [x] Bidirectional slider mapping (setRangeValue / adjustRangeValue)
- [x] Bidirectional color mapping (setColor)
- [x] Outbound sensor reporting (currentTemperature, with humidity auto-discovery)
- [x] Per-capability mapping — optional explicit capability targeting with type-safe resolution (falls back to auto-discovery on type mismatch)
- [x] Secret encrypted at rest (same `enc:v1:` format as MQTT password)
- [x] Device-online check before dispatching inbound voice commands
- [x] Web dashboard Sinric status section (connection dot, message counters, mapping breakdown)
- [x] User guide §22 — complete setup walkthrough
- [x] **Scene triggering via voice (post-v0.8.0)** — map a Sinric virtual device to a Trellis scene so "Alexa, turn on Movie Mode" fires `run_scene`. `scene_id` field on `SinricDeviceMapping` (`serde(default)` for backward compat). `setPowerState(On)` runs all scene actions sequentially via ConnectionManager; `setPowerState(Off)` responds success (no-op). Settings UI has a Device/Scene type toggle per mapping row with scene dropdown. Scene execution uses same DB + ConnectionManager path as REST API and desktop app.

## Automation

- [x] Scheduled actions (cron-based: "turn on pump at 6am daily") — supports both single device/capability actions and full scene execution
- [x] **Scene scheduling (post-v0.7.0)** — schedules can fire entire scenes on a cron schedule. Schedule creation form has a type toggle (Single Action / Scene). `scene_id` column on `schedules` table. Scheduler loads all scene actions and executes sequentially. Scene name shown in schedule cards in both desktop app and web dashboard.
- [x] **Schedule insight — next-run preview + Run now (v0.22.0)** — every schedule card shows a human-friendly `Next: Today at 14:30 / Tomorrow at 06:00 / Mon Apr 28 at 09:00` line derived from the cron expression (`scheduler::compute_next_run` using the same `cron` crate the evaluator uses). New `run_schedule` Tauri command + admin-gated `POST /api/schedules/:id/run` fire the schedule's preset action immediately, stamping `last_run` just like the cron loop. Execution path factored out of `evaluate_schedules` into `fire_schedule(app, conn, schedule)` so manual-run and cron-run share one path (single-action + scene both covered). Desktop Automation → Schedules tab gains a ▶ icon button; `:9090` web UI adds a "Run now" button (admin-only, viewer hides). Disabled schedules have the button greyed out and their Next: line hidden (disabled ⇒ no cron firing planned). Invalid cron expressions render `Next: —`.
- [x] Conditional rules ("if temp > 30, turn on fan")
- [x] Rule evaluation engine (checks on sensor updates, 30s debounce)
- [x] **Compound rule conditions with AND/OR logic (post-v0.9.0)** — rules support multiple conditions combined with AND or OR logic. Each condition can reference a different device and sensor, enabling cross-device rules like "if temp > 30 AND humidity < 40, turn on fan". Four operators: above, below, equals, not_equals. Multi-condition form in desktop app with add/remove condition rows and AND/OR toggle. Evaluation reads current device states from the Zustand store for cross-device conditions (offline devices' conditions evaluate as "not met"). Legacy single-condition rules continue to work via fallback. New `logic` and `conditions` (JSON) columns on the rules table. Config import/export preserves compound conditions. Web dashboard shows compound conditions with AND/OR labels.
- [x] **Rule observability — Last triggered + Run now (post-v0.22.0)** — closes the silent-operation gap on the Automation → Rules tab (direct parallel to v0.22.0 Schedule insight for Schedules). Rules fire from `deviceStore.ts::checkRules` (frontend-side condition eval against live sensor data) but previously had no persisted trace: the only signal a rule was working was watching the target device change. New `last_triggered TEXT` column on `rules` via additive ALTER TABLE migration; `Database::get_rule(id)` + `update_rule_last_triggered(id)` mirror the schedule helpers. Shared Rust `scheduler::fire_rule(app, conn_mgr, rule)` mirrors `fire_schedule`: routes through the existing `send_action` helper (target IP/port from `get_saved_device`), stamps `last_triggered = datetime('now')` on success. New admin-gated `run_rule(id)` Tauri command + `POST /api/rules/:id/run` REST endpoint (admin-gated, LAN returns 401 like other mutating rule routes). Frontend `checkRules` now invokes `run_rule` instead of calling `send_command` directly — centralizes the fire path so condition-matched + manual fires go through the same code, and both stamp `last_triggered`. Desktop Automation Rules card gains a ▶ Play icon (disabled when rule disabled, title "Run now (bypass conditions)") and a `Last triggered: YYYY-MM-DD HH:MM:SS` line beneath the rule detail. `:9090` web UI adds the same "Run now" button (admin-only, viewer hides when `isViewer`) + Last triggered line, matching the schedule card layout. Run now deliberately bypasses condition evaluation — the point is to verify the target-action wiring without waiting for a real trigger. 30s frontend debounce for condition-matched fires is unchanged (manual Run now isn't debounced — admin opt-in). 3 new unit tests (`get_rule_returns_none_for_missing_id`, `new_rule_has_null_last_triggered`, `update_rule_last_triggered_stamps_timestamp`) — 133 → 136 library tests. Hardware-verified live on trellis-fccfb7c8: REST manual fire → LED on + `last_triggered` stamped; follow-up rule with matching condition → event-driven fire invoked `run_rule` from the frontend → LED off + `last_triggered` stamped 8s later after the next sensor broadcast.
- [x] **Rule management parity on `:9090` web UI — toggle + delete (post-v0.22.0)** — closes the pre-existing web/desktop gap that the post-v0.22.0 Rules observability feature deliberately skipped to keep scope tight. Admins had to switch to the desktop app just to enable/disable or delete a rule. New admin-gated `PATCH /api/rules/:id/enabled` REST endpoint accepts `{"enabled": bool}` and delegates to the existing `Database::toggle_rule`; rejects malformed bodies with 400 "Body must be {\"enabled\": bool}" and non-numeric ids with 400 "Invalid rule ID". `DELETE /api/rules/:id` was already in place. `:9090` Rules card grows an **Enable**/**Disable** button (label flips based on current state) and a × delete button beside the Run now button; both hidden when `isViewer`. Delete confirms via `confirm()` before firing. New `toggleRule(id, enabled, btn)` + `deleteRuleFromDashboard(id)` JS handlers parallel the existing `runRule` shape (toast on success, `loadAutomation()` to refresh). CORS preflight `Access-Control-Allow-Methods` widened to include `PATCH` alongside `PUT`. 2 new db tests (`toggle_rule_flips_enabled_state`, `delete_rule_removes_row`) — 139 → 141 library tests. Hardware-verified via REST on trellis-fccfb7c8: rule created → PATCH false → disabled → PATCH true → re-enabled → DELETE → list empty; LAN-origin PATCH correctly 401s.
- [x] **Webhook observability — last delivery + success/failure counts (post-v0.23.0)** — closes the "is this webhook actually working?" signal gap on the Automation card surface, continuing the observability arc from v0.22.0 Schedule next-run and v0.23.0 Rule last-triggered. `get_webhooks()` SQL extends with four correlated subqueries on `webhook_deliveries` (last delivery timestamp, last-delivery success flag, success count, failure count) — zero extra endpoints, zero N+1 at the card surface. `Webhook` struct grows matching `last_delivery: Option<String>`, `last_success: Option<bool>`, `success_count: i64`, `failure_count: i64` fields (all `#[serde(default)]` so POST/toggle paths that don't set them stay compatible). Desktop Automation → Webhooks card header adds a compact `Last delivery: <ts> · 3✓ / 2✗` line beneath the URL row; text tints red when the most recent delivery failed. `:9090` embedded web UI matches the same line in the card detail area below URL/Event. Zero-delivery webhooks correctly omit the line (conditional on `success_count + failure_count > 0`). Expandable full delivery log on desktop stays unchanged — this feature is the at-a-glance signal. 3 new db tests (`new_webhook_has_null_summary_fields`, `get_webhooks_aggregates_delivery_stats`, `get_webhooks_last_success_reflects_most_recent`) — 145 → 148 library tests. Hardware-verified end-to-end via headless Chrome CDP: fixture webhook with 3 successful + 2 failed deliveries seeded into SQLite, page rendered with correct counts, red tint applied to `last_success=false` line, companion zero-delivery webhook correctly omitted the line. Third reuse of the CDP end-to-end pattern — crystallized as `skill_headless_chrome_cdp_test.md`.
- [x] **Scene observability — Last run on cards (post-v0.23.0)** — closes the fourth and final hole in the automation-parity arc: schedules got `last_run` in v0.22.0, rules got `last_triggered` in v0.23.0, webhooks got `last_delivery` + counts post-v0.23.0 — scenes stayed silent until now. New `last_run TEXT` column on `scenes` via additive ALTER TABLE migration; `Database::get_scene(id)` + `get_scenes()` SELECT it; new `update_scene_last_run(id)` helper. Shared Rust `scheduler::fire_scene(app, conn_mgr, scene)` mirrors `fire_rule`/`fire_schedule`: iterates actions through `send_action`, stamps `last_run = datetime('now')` when at least one action succeeded (matching prior semantics — a scheduled scene run already stamped the schedule's `last_run` via the old code). Three fire paths now collapse into this one helper: the Tauri `run_scene` command (commands.rs, no longer async), the REST `POST /api/scenes/:id/run` (api.rs), and `run_scene_from_sinric` (voice assistant path). `fire_schedule`'s scene branch also delegates to `fire_scene` — a scheduled scene now stamps BOTH `schedule.last_run` AND `scene.last_run` in one shot. Desktop `Scenes.tsx` card gains a faint `Last run: YYYY-MM-DD HH:MM:SS` line under the action summary (omitted when `last_run` is null). `:9090` embedded web UI scene card mirrors the same line style used by the sibling schedule/rule cards. Zero-run scenes omit the line on both surfaces. 3 new db tests (`new_scene_has_null_last_run`, `update_scene_last_run_stamps_timestamp`, `get_scenes_exposes_last_run_on_every_row`) — 148 → 151 library tests. Hardware-verified end-to-end via headless Chrome CDP (fourth reuse of `skill_headless_chrome_cdp_test.md`): fixture scene created via REST, `sqlite3` seeded `last_run='2026-04-24 10:30:00'`, Scenes tab rendered the line with the seeded timestamp, `POST /api/scenes/:id/run` fired the ESP32 `led=false` and stamped a fresh today-timestamp, companion zero-run scene correctly omitted the line. 4 DOM assertions + 3 REST round-trips all passed.
- [x] **Schedule + webhook management parity on `:9090` web UI — toggle + delete (post-v0.23.0)** — closes the remaining parity gap on the Automation card surface, symmetric to v0.23.0's Rules parity. Two new admin-gated PATCH endpoints: `PATCH /api/schedules/:id/enabled` and `PATCH /api/webhooks/:id/enabled`, each accepting `{"enabled": bool}` and delegating to the existing `Database::toggle_schedule` / `toggle_webhook`. Malformed body → 400 "Body must be {\"enabled\": bool}"; non-numeric id → 400. Schedule card grows Enable/Disable + × delete beside the Run now button; webhook card grows Enable/Disable + × delete beside the Test button. All four are viewer-hidden; delete confirms via `confirm()`. Four new JS handlers (`toggleSchedule`, `deleteScheduleFromDashboard`, `toggleWebhook`, `deleteWebhookFromDashboard`) parallel the existing `toggleRule`/`deleteRuleFromDashboard` shape. 4 new db tests (`toggle_schedule_flips_enabled_state`, `delete_schedule_removes_row`, `toggle_webhook_flips_enabled_state`, `delete_webhook_removes_row`) — 141 → 145 library tests. Hardware-verified end-to-end via headless Chrome CDP drive on real `:9090`: test schedule + webhook created via REST → click Disable → PATCH false → card rerender with Enable button + disabled badge + Run now hidden → click Enable → PATCH true → × Delete (confirm stub) → DELETE → gone. 8 DOM assertions + 4 REST round-trips all passed.
- [x] **Scene MQTT discovery — HA button entities (post-v0.24.0)** — every Trellis scene now appears in Home Assistant as a `button` entity, so HA dashboards / voice routines (Alexa via HA, Google via HA) / automations can fire any scene without going through the Trellis UI. Closes the scene side of the v0.22.0 energy→HA arc: capabilities, sensors, power, and energy were already discoverable; scenes were the last automation surface still invisible to HA. New `MqttBridge::publish_scene_discovery(scene_id, name)` / `publish_all_scene_discovery()` / `forget_scene_discovery(scene_id)` mirror the existing per-device discovery API but emit `<ha_prefix>/button/trellis_scene_<id>/config` retained configs with `payload_press: "PRESS"` and `command_topic: <base_topic>/_scene/<id>/run`. All scene buttons share one synthetic `trellis_scenes` device block ("Trellis Scenes" / "Scene") so HA collapses them under one card. Underscore-prefixed `_scene` namespace keeps the topic disjoint from device IDs (which always start with `trellis-`). The MQTT worker subscribes to `<base_topic>/_scene/+/run` on connect AND on every ConnAck reconnect (mirrors polish #5 for the cap-set pattern); a press dispatches via new `dispatch_scene_run` → `Database::get_scene` → `scheduler::fire_scene` (same shared scene runner as Tauri/REST/Sinric paths, so `last_run` stamping behaves identically). Bridge stays database-free on the hot publish path — DB access happens only on inbound dispatch (cold path) and on bulk republish (cold path), through a new `app_handle: Arc<Mutex<Option<AppHandle>>>` field wired in `lib.rs` after construction (mirrors the Sinric bridge pattern). All four scene CRUD paths (Tauri `create_scene` / `update_scene` / `delete_scene` / `duplicate_scene` and the four matching REST endpoints) call `publish_scene_discovery` / `forget_scene_discovery` so HA stays in sync without a bridge restart — no-op when discovery is off. Republish dedupe keys on `(scene_id, name)`: a no-op save sends nothing on the wire, a rename republishes so HA picks up the new label. Forget-on-delete emits an empty retained payload (HA's idiomatic entity-removal sentinel). On broker reconnect (ConnAck branch in the worker), scene discovery is republished alongside device discovery so a broker restart that wipes retained state recovers cleanly. 9 new unit tests (`scene_run_topic_parses_to_dispatch`, `scene_run_topic_rejects_non_numeric_id`, `scene_run_topic_rejects_extra_segments`, `scene_run_topic_rejects_device_set_pattern`, `scene_discovery_dedupe_skips_repeat_with_same_name`, `scene_discovery_dedupe_republishes_on_rename`, `forget_scene_drops_tracker_entry`, `scene_discovery_topic_uses_button_component`, `scene_command_topic_uses_underscore_namespace`) — 163 → 172 library tests. Hardware-verified end-to-end against the live `localhost:1883` broker: post-relaunch `mosquitto_sub` confirmed retained `homeassistant/button/trellis_scene_{2,4}/config` configs for both pre-existing scenes; `mosquitto_pub trellis/_scene/{2,4}/run PRESS` fired the scenes (REST `last_run` field updated for both); REST `POST /api/scenes` for a new "MQTT Smoke Test" scene immediately published a `trellis_scene_9` config; `PUT` rename republished with the new name; `DELETE` cleared the retained payload (only scenes 2 + 4 remained on the broker afterward). End-to-end CRUD round-trip across REST + MQTT = clean.
- [x] **Rules can fire scenes (post-v0.25.0)** — closes the last hole in the "every automation surface fires scenes" arc. Schedules got scene-firing in v0.7.0, Sinric voice in post-v0.8.0, MQTT/HA buttons in v0.25.0 — rules were the last single-action-only surface. New `scene_id INTEGER REFERENCES scenes(id)` column on `rules` via additive ALTER TABLE migration; `Rule` struct grows a matching `scene_id: Option<i64>` (`#[serde(default)]` for back-compat with imported configs). `scheduler::fire_rule` branches on `scene_id` exactly like `fire_schedule` does — when set, loads the scene and delegates to the shared `fire_scene` helper, then stamps `rule.last_triggered`; the scene path also stamps `scene.last_run` so a single fire updates BOTH timestamps in the same datetime('now') second. Single-action path unchanged. `create_rule` Tauri command + `handle_create_rule` REST handler thread `scene_id`; `duplicate_rule` preserves it; config import/export remaps via `sceneIdMap` (mirrors the schedule-scene-link import path). Desktop `Automation.tsx` Rule form adopts the schedule pattern: type toggle (Single Action / Scene), scene dropdown when picked, Zap-icon prefix on scene-targeted rule cards, body line collapses to `→ Scene: <name>` instead of `→ device.cap = value`. Embedded `:9090` web UI: rule cards show the bolt prefix when `scene_id != null`, and `formatRuleCondition(r, sceneMap)` renders `→ ⚡ Scene: <name>` (the `:9090` Automation tab doesn't expose rule creation yet, intentionally — desktop is where rules are authored). **Latent bug fixed along the way:** v0.7.0's scene-targeted schedules path has been broken in production since shipping. Schema had `device_id TEXT NOT NULL FOREIGN KEY → devices(id)`; FK enforcement is on by default in `libsqlite3-sys` (bundled), so the desktop UI sending `deviceId: ""` for the scene case was rejected with `FOREIGN KEY constraint failed` — production DB confirms zero scene-targeted schedules ever existed despite the feature being documented as shipped. Bundled migration `rebuild_drop_target_device_fks` rebuilds both `rules` (drops FK on `target_device_id`) and `schedules` (drops FK on `device_id`) via the SQLite-recommended CREATE _new + INSERT + DROP + RENAME flow with `PRAGMA foreign_keys=OFF` toggled around the rebuild; idempotent (gated on `pragma_foreign_key_list` reporting the FK still in place); preserves the scene_id REFERENCES + the source_device_id FK on rules. 5 new lib tests (`new_rule_has_null_scene_id`, `create_rule_with_scene_id_persists`, `duplicate_rule_preserves_scene_link`, `migration_drops_target_device_fk_on_rules_and_schedules` exercising pre-FK-violation → post-acceptance + row preservation + source-FK still-in-force, `migration_is_idempotent`) — 175 → 180 library tests. Hardware-verified live on trellis-fccfb7c8: scene rule POST `/api/rules` with `scene_id=4`, target_*=""; `POST /api/rules/1/run` → ESP32 LED True → False (scene action `led=false` actuated); `rule.last_triggered` and `scene.last_run` both stamped fresh at the same datetime('now') second confirming the shared `fire_scene` path. Headless Chrome CDP smoke against real `:9090` confirmed the rule card renders `⚡ Scene rule smoke` title + `If Greenhouse Controller.temp above 99 → ⚡ Scene: AnotherAlandaLA Test` body + `Last triggered: 2026-04-26 …` line. Pre-migration DB backed up at `/tmp/trellis.db.pre-v0.26.0-rules-scenes-1777182431` (32 MB).
- [x] **Copy duplicate action across automation surfaces (post-v0.24.0)** — every automation surface (schedules, rules, webhooks, scenes) gains a one-click **Copy** action that creates an editable duplicate so iterating on a working configuration stops requiring users to re-enter every field by hand. Four new admin-gated REST endpoints (`POST /api/{schedules,rules,webhooks,scenes}/:id/duplicate`) return `{"id": <new_id>}`; four matching Tauri commands (`duplicate_schedule` / `duplicate_rule` / `duplicate_webhook` / `duplicate_scene`) wired into the desktop app. Backend implemented as `Database::duplicate_*` methods that read the source via `get_*` and call `create_*` with a shared `copy_label(src)` free helper appending ` (copy)` (or just `(copy)` when the source is empty/whitespace). Each duplicate starts **enabled** regardless of source state — copying is the first step toward a working variant, so flipping disable→enabled saves a click; observability state (`last_run` / `last_triggered` / `last_delivery`, success/failure counters) explicitly resets to NULL/0 so a fresh copy doesn't inherit stale history. Scene duplicate copies all `scene_actions` rows in their original `sort_order`. New `get_webhook(id)` getter introduced as part of the duplicate path (no longer a list-only resource at the DB layer). Desktop `Automation.tsx` adds a generic `handleDuplicate(type, id)` mirroring the existing `handleToggle`/`handleDelete` shape — single Copy icon button (lucide `Copy`) sits between Run-now and Toggle on schedule/rule/webhook cards. `Scenes.tsx` adds a labeled "Copy" button between Edit and Run. `:9090` embedded web UI adds four matching JS handlers (`duplicateSchedule` / `duplicateRule` / `duplicateWebhook` / `duplicateScene`) and Copy buttons on every card; viewer role hides them all. 12 new unit tests (`copy_label_appends_suffix_and_handles_blank`, `duplicate_schedule_creates_independent_row`, `duplicate_schedule_preserves_scene_link`, `duplicate_schedule_404s_on_missing`, `duplicate_rule_creates_independent_row_with_conditions`, `duplicate_rule_404s_on_missing`, `duplicate_webhook_creates_independent_row`, `duplicate_webhook_handles_null_device_id`, `duplicate_webhook_404s_on_missing`, `duplicate_scene_copies_actions_and_resets_last_run`, `duplicate_scene_preserves_action_order`, `duplicate_scene_404s_on_missing`) — 151 → 163 library tests. Hardware-verified end-to-end via headless Chrome CDP drive against real `:9090`: one fixture per surface created via REST, Copy clicked on each card → REST round-trip confirms `(copy)` row exists with `enabled=true` and (for scenes) all 2 actions preserved → DOM rerender shows the duplicate card alongside the source → all 8 fixtures cleaned up.
- [x] Webhooks (POST to URL on device_offline, device_online, alert_triggered, sensor_update)
- [x] **Webhook retry, delivery log, and test button (post-v0.9.0)** — webhooks retry up to 3 times with exponential backoff (2s, 4s, 8s) on failure. Each delivery attempt logged in `webhook_deliveries` SQLite table (webhook_id, event_type, status_code, success, error, attempt, timestamp). Desktop app: expandable delivery log per webhook showing last 20 deliveries with status codes, retry indicators, and timestamps. Test button sends a synthetic POST and records the result. Web dashboard: Test button on webhook cards. REST endpoint `GET /api/webhooks/{id}/deliveries` returns delivery history.
- [x] **Webhooks actually fire on real events — backend dispatcher (post-v0.25.0)** — closes a "shipped UI without backend" gap that had been open since v0.9.0+: webhooks had a full UI (event-type dropdown, CRUD, manual Test button, observability pills, deliveries log) but **no backend code matched event_type rows against real events** — the Test button POSTed from the *browser*, not the backend. New `webhooks::dispatch_event(app_handle, event_type, device_id, payload)` synchronously looks up matching rows (returns instantly when nothing matches), then spawns a worker thread that POSTs each match with a 10s timeout via `ureq` and writes to `webhook_deliveries` from the same connection — existing observability lights up automatically. New `Database::get_webhooks_for_event` query handles dot/underscore back-compat (`event_type = ?1 OR event_type = ?2`) so legacy rows fire post-upgrade without recreation. Wired at three emit sites: discovery.rs mDNS service-removed (`device.offline`), discovery.rs health-check came-online (`device.online`) + went-offline (`device.offline`), api.rs OTA ack (`ota_applied`). Dropdown values switched to dot form; OTA option added. 180 → 187 lib tests (6 new on get_webhooks_for_event: dot match + back-compat both directions + device-id filter + disabled skip + no-event-device matches NULL only + other-event skip; +1 normalize_event_type unit test). Hardware-verified live on trellis-fccfb7c8: power-cycle drove the full sequence — relaunch → device.online (200), unplug → device.offline (200), replug → device.online (200) — all three deliveries logged with correct status, both webhook cards' observability pills updated.
- [x] **Backend alert evaluator + sensor.update dispatcher (post-v0.25.0)** — closes the v0.26.0 webhook arc and the second-shape "evaluator only ran in the browser" gap. The `alerts` table + REST + Tauri commands had shipped since v0.4-ish, but evaluation lived in frontend `checkAlerts` only — closed desktop = silent alerts, no ntfy push, no webhooks. New `alerts.rs::evaluate(handle, device_id, metric_id, value)` reads enabled alerts via `get_alerts`, evaluates above/below, debounces 60s per `alert_id` in a static `Mutex<Option<HashMap<i64, Instant>>>`, and on trigger fans out: (a) `webhooks::dispatch_event("alert.triggered", ...)` with `{alert_id, label, metric, value, condition, threshold, device_name}`, (b) ntfy push when `ntfy_topic` setting is configured (10s timeout). Wired at `connection.rs::update`'s numeric-value branch alongside a parallel `webhooks::dispatch_event("sensor.update", ...)` carrying `{metric, value}`. Both surfaces filter naturally — webhook needs an event_type row, alerts need `(device_id, metric_id)` rows — non-matching = no work. **Frontend dedupe:** deleted `fireWebhooks` + `attemptWebhook` (~67 LOC) since slot-2 + slot-3 backend are now authoritative — fixes a slot-2 latent regression where legacy underscore webhook rows would have fired *twice* (once browser, once backend). Kept frontend `checkAlerts` desktop-notification path (browser permission flow); removed its ntfy duplicate. Dropdown "(planned)" suffix removed for both events. 187 → 192 lib tests (5 new: condition_matches above/below/unknown + debounce blocks-immediate-refire + per-alert-independence). Hardware-verified live on trellis-fccfb7c8: relaunch fired sensor.update for temp/humidity/brightness (all 200), alert.triggered fired once for temp 22.4 > 10 with full payload, then four subsequent temp readings (25.1, 22.9, 25.7, 29.0, 23.8) sent zero duplicate alert.triggered events while sensor.update kept flowing — debounce confirmed end-to-end.
- [x] Device templates (save/load capability configs for firmware generator)
- [x] CSV data export (download sensor history from charts)
- [x] Integrated terminal (run shell commands, arrow-key history)

## Data Management

- [x] Config import/export (full backup of devices, scenes, schedules, rules, webhooks, alerts, templates, groups)
- [x] **Config import/export v2 (post-v0.8.0)** — export now includes backend-backed scenes (SQLite, not stale localStorage), floor plans, device positions, and favorite capabilities. Import remaps IDs across cross-referenced tables (groups, scenes, floor plans) so references survive the export/import round-trip. Schedule import preserves `scene_id` for scene-type schedules. Config version bumped to 2; backward-compatible with old v0.1.5 exports (missing sections silently skipped).
- [x] **Configurable data retention (v0.4.6)** — metrics and device logs cleanup period selectable from Settings: 7 days, 30 days (default, backward-compatible), 90 days, 1 year, or forever (disables cleanup). Cleanup thread reads the setting each hourly cycle. Dropdown in both the desktop Settings page and the `:9090` web dashboard. Viewers can see but not change the setting.
- [x] **Configurable webhook delivery retention (post-v0.24.0)** — sibling to data retention: prunes rows from `webhook_deliveries` (the table that backs each webhook card's last-delivery timestamp + success/failure counts) so the table stops growing forever. New `webhook_delivery_retention_days` setting selectable from 7 / 30 (default) / 90 / 365 / 0 (Forever) on both the desktop Settings page (Data Retention card) and the `:9090` Data Management section. Hourly cleanup loop in `lib.rs` adds a third call to `Database::cleanup_old_webhook_deliveries(days)` alongside the existing `cleanup_old_metrics` + `cleanup_old_logs` siblings. `capability_state_log` is intentionally NOT swept — `get_capability_lifetime_wh` integrates from `MIN(timestamp)` for HA's `total_increasing` Wh sensor, so deleting old rows would shift the floor and HA would treat it as a counter reset. 3 new lib tests (172 → 175). Hardware-verified end-to-end via headless Chrome CDP: navigate to Settings → DOM read confirms select id=`webhookRetentionSelect`, all 5 options, default 30, label + description rendered → change to 90 → REST round-trip returns `{"value":"90"}` → change to 0 (Forever sentinel) → REST returns `{"value":"0"}` → restore.
- [x] **Energy usage tracking (post-v0.18.0)** — per-switch nameplate watts (`capability_meta` table) combined with WS-captured state transitions (`capability_state_log` table, deduped against previous row). `Database::get_device_energy` bootstraps from the latest row before window_start (assume OFF when none), walks in-window transitions, accrues on-time × watts / 3600 → Wh, closes the open interval at now. Tauri `get_device_energy` + REST `GET /api/devices/:id/energy?hours=N` (viewer-safe). Desktop: `CapabilityWatts` inline ⚡ editor under each switch in Device Detail; `DeviceEnergy` section between Uptime History and Sensor Charts with 24h / 7d / 30d toggle, per-capability breakdown when >1 metered switch, and "tracking since" hint. Settings → Data → Energy Tariff adds optional `cost_per_kwh` + currency (USD/EUR/GBP/CAD/AUD/JPY/RON); when set the card appends `≈ <currency> <cost>`. Switches only for now (sliders phase 2); desktop-only for now (web `:9090` + MQTT/HA parity phase 2).
- [x] **Energy tracking — online-aware on-time intersection (v0.20.0)** — fixes a correctness gap in `get_device_energy`: capability on-intervals were previously summed verbatim even when the device was offline mid-interval (a switch held ON across an offline window would have been credited for the full duration even though the desktop had no proof the device stayed energized). New pure-function helpers `compute_offline_intervals` + `compute_on_seconds_online` in `db.rs` derive offline windows from `device_logs severity='state'` rows and subtract their overlap from each capability's on-intervals. Non-regression: capabilities whose on-intervals fall entirely inside online windows return unchanged on_seconds. Live probe: synthetic 9090s ON-interval with a 9039s offline overlap → endpoint correctly reports 51s (pre-fix would have reported 9090s). 10 new unit tests covering full-overlap, partial-leading, partial-trailing, encompassing, disjoint, multi-offline, and zero-overlap cases (90 → 100 lib).
- [x] **Energy tracking — embedded web dashboard parity (v0.20.0)** — brings the v0.19.0 desktop energy-tracking surface to the `:9090` embedded web dashboard verbatim. Inline ⚡ watts editor renders under each switch in Device Detail (admin-editable, viewer-readonly via the existing role gate), Energy card slots between Controls and Links with the same 24h/7d/30d toggle, per-capability breakdown when >1 metered switch, and `≈ <currency> <cost>` line when a tariff is set. Settings → Energy Tariff exposes `cost_per_kwh` + currency dropdown (USD/EUR/GBP/CAD/AUD/JPY/RON) for setting/clearing the tariff. Zero backend change — all REST endpoints (`GET /api/devices/:id/energy`, `PUT .../capabilities/:cap/meta`, settings tariff) were already in place from v0.19.0. 13/13 headless-Chrome probes passed across desktop (1280px) and mobile (375px, no horizontal overflow).
- [x] **Energy tracking — HA discovery power sensor per metered switch (v0.20.0)** — every switch with `nameplate_watts` set in `capability_meta` now publishes a companion `sensor.<cap>_power` entity via the existing MQTT bridge for Home Assistant auto-discovery. Entity is `device_class=power, state_class=measurement, unit_of_measurement=W`. New in-memory `watts_map` cache on `MqttBridge` is hydrated from `capability_meta` at startup and kept in sync via a new `set_watts()` method wired into both the Tauri `set_capability_watts` command and the REST `PUT /api/devices/:id/capabilities/:cap/meta` endpoint. The discovery publish emits the entity config plus a retained initial `0` state; on every WS state update for a bool-valued switch, `publish_state` mirrors the new state as W (ON → nameplate_watts, OFF → 0). Clearing a switch's watts publishes an empty retained payload to the config topic — the Home Assistant idiom for "remove this entity" — so unmetered switches don't leave dangling entities. 6 new unit tests (format_watts_payload whole + decimal, compute_power for ON/OFF/non-bool, hydrate + set cache ops). Live mosquitto_sub probe on real ESP32 (trellis-fccfb7c8/led at 60W): full discovery → `_power/state 0` → ON → `_power/state 60` → OFF → `_power/state 0` → clear watts → entity removed → restore 60W → re-published. The total_increasing Wh sensor for HA's Energy dashboard remains BACKLOG'd (retention-horizon caveat — ship when a user asks).
- [x] **Energy tracking — HA `total_increasing` Wh sensor per metered capability (v0.22.0)** — completes the v0.19.0/v0.20.0 energy-tracking story for Home Assistant. v0.20.0 shipped the instantaneous `sensor.<cap>_power` entity; this adds the cumulative counter HA's **Energy dashboard** needs: a companion `sensor.<cap>_energy` entity auto-discovered for every capability that has `nameplate_watts` set AND is either a switch or a slider opted into `linear_power` (`device_class=energy`, `state_class=total_increasing`, `unit_of_measurement=Wh`). New `Database::get_capability_lifetime_wh(device, cap)` integrates every logged transition in `capability_state_log` from its `MIN(timestamp)` onward — `capability_state_log` is NOT under retention cleanup, so the window lower bound is stable across the device's full lifetime and the counter stays monotonic (modulo user edits to `nameplate_watts` / `slider_max`, which HA treats as counter resets per the `total_increasing` spec). Integration reuses the existing online-aware helpers (`compute_offline_intervals` + `compute_on_seconds_online` + `compute_numeric_wh_online`); returns `0.0` cleanly when the cap has no meta or no rows, letting callers publish unconditionally. `MqttBridge` refactors `watts_map` into a richer `meta_map: HashMap<(device, cap), MeteredMeta { watts, linear_power, slider_max }>`, hydrated at startup from a new `get_all_capability_meters` query. New `publish_energy(device, cap, wh)` + `metered_capabilities()` keep the bridge database-free; Wh computation happens at the call site — `connection.rs` after every logged transition plus a dedicated 60-second periodic-tick thread in `lib.rs` that keeps HA's hourly Energy buckets accurate during long steady-state intervals. Discovery publish emits both the `_power` + `_energy` config topics; clearing watts (`set_watts(None)`) or opting a slider out of `linear_power` unpublishes both config topics via the HA retain-empty idiom. Side effect intentional: the `meta_map` refactor also closes the phase-2 gap where linear-power sliders had no `_power` HA sensor — sliders now get both `_power` and `_energy` entities alongside switches, with slider `_power` payloads computed as `(value/slider_max) × watts` (non-opted sliders still skip `_power` to avoid noise). 11 new unit tests (`get_capability_lifetime_wh` switch path / slider path / no-meta / no-rows / no-nameplate, plus payload helpers for slider linear fraction / slider_max fallback / Wh integer + fractional trim) — 117 → 128 library tests. Scope line: non-linear power curves (LED gamma) and current-sense hardware `energy_meter` remain future work; this is the straight-line cumulative over data already being logged.
- [x] **Energy tracking — sliders phase 2, linear-power opt-in (v0.22.0)** — closes the v0.19.0 data-correctness gap where slider-controlled loads (LED dimmers, PWM heaters) contributed 0 Wh because only bool-valued `update` events were persisted to `capability_state_log`. `capability_meta` grows two columns — `linear_power INTEGER NOT NULL DEFAULT 0` and `slider_max REAL` — via additive ALTER TABLE migrations; new admin-gated Tauri command `set_capability_linear_power(deviceId, capabilityId, linearPower, sliderMax)` plus the existing REST `PUT /api/devices/:id/capabilities/:cap/meta` endpoint extended to accept `linear_power` + `slider_max` alongside the existing `nameplate_watts` in a single body. `connection.rs::update` handler routes numeric values through new `Database::log_slider_value_if_linear`, which inline-checks `linear_power=1 AND nameplate_watts IS NOT NULL` (no-op for non-opted sliders — zero write amplification outside the opt-in set), rounds f64 to i64 (slider NVS persistence is integer), then dedups + writes. `get_device_energy` branches per-cap: switches stay on the bool `compute_on_seconds_online × watts / 3600` math; opted-in sliders integrate via new `compute_numeric_wh_online(bootstrap, rows, window_start, now, offline_intervals, watts, max)` which walks each `[last_ts, ts)` segment, subtracts offline overlap, and accrues `watts × (value/max_safe) × online_dt / 3600` (max_safe falls back to 255 when `slider_max` is null or non-positive). `on_time_seconds` for sliders means "seconds at non-zero value within online windows" — semantically consistent with the switch path. Desktop `CapabilityWatts` component gains a `capType`/`linearPower`/`sliderMax` prop set + a "Linear power" checkbox visible only when `capType === 'slider'`, with an inline one-line caveat about resistive loads vs LEDs/motors; saves POST the full meta body in a single write. `DeviceDetail` loads both fields from `get_device_capability_meta`, passes `cap.max` (live Capability max) at save time, extends the Energy-card visibility predicate to also fire when at least one slider has opted in, and stops filtering `capabilityLabels` to switches-only. `:9090` web UI parity: slider controls now render the same `capWatts-*` row (via a new `data-slider-max` attribute), `renderCapWatts` emits the checkbox + hint + `· linear` badge when opted in, `saveCapWatts` bundles `linear_power` + `slider_max` into the PUT body. Five new pure-function unit tests for `compute_numeric_wh_online`: constant half-value / stepped transitions / offline-overlap subtraction / zero-bootstrap empty-rows / slider_max fallback — 112 → 117 library tests. Verified live on trellis-fccfb7c8 (brightness slider, max=100, 15W opted in): REST PUT landed `linear_power=true, slider_max=100`; command-scrub 75→25→75→25→100→0→80→40 persisted 8 `capability_state_log` rows with correct deduping; `/api/devices/.../energy?hours=24` reported 0.133 Wh for brightness (57s on-time, matches hand-computed integration to ~2%). Scope line intentionally drawn: non-linear power curves (LED gamma, motor torque) remain BACKLOG'd phase 3 pending a real use-case; real current-sense hardware path is the long-tail `energy_meter` capability type already filed.

- [x] **Energy tracking — Lifetime readout on desktop + :9090 (post-v0.22.0)** — v0.22.0 shipped a lifetime cumulative `_energy` counter in the MQTT/HA bridge, but the desktop Energy card and the `:9090` web dashboard only offered 24h / 7d / 30d windows — users watching the same device in HA's Energy dashboard couldn't see the same lifetime total on the control app itself. New fourth `Lifetime` entry in the Energy range toggle pulls the same cumulative integration the MQTT side uses: new `Database::get_device_lifetime_energy(device_id)` iterates every metered capability (switch OR slider with `linear_power`) and returns a `DeviceEnergyReport` with `window_hours=0` (sentinel for lifetime mode) and per-cap `on_time_seconds` + `wh` + `tracked_since`. The existing `get_capability_lifetime_wh` is refactored to delegate to a shared `compute_capability_lifetime(&Connection, device, cap)` free helper so the MQTT path (`connection.rs` per-transition + 60s periodic tick in `lib.rs`) keeps working byte-identically — both paths now flow through the same computation, guaranteed to produce the same total. New Tauri command `get_device_lifetime_energy` + pre-auth'd `GET /api/devices/:id/energy/lifetime` REST endpoint (viewer-safe, same role gate as the windowed endpoint). Desktop `DeviceEnergy.tsx` swaps its `hours` state for a `RangeKey` (`"24h" | "7d" | "30d" | "lifetime"`) and branches invoke between `get_device_energy` and `get_device_lifetime_energy` based on selection; `fmtOnTime` extended to render ≥24h as `Nd Nh` so lifetime on-times don't collapse to four-digit hours. `:9090` web UI adds a fourth button with `data-hours="0"` and `setEnergyRange(0)`; `loadDeviceEnergy` branches to `/energy/lifetime` when the selected range is 0; `fmtOnTimeSec` gets the same days-aware extension. The existing "Tracking since …" hint becomes doubly meaningful in lifetime mode since it IS the window. 3 new unit tests on `get_device_lifetime_energy` (empty-device → 0 caps / aggregates per-cap switch + linear slider / skips unmetered) — 136 → 139 lib tests. Verified live on trellis-fccfb7c8 in lifetime mode: led (60W, tracked since 2026-04-21) = 482s on × 60W / 3600 = 8.033 Wh ✓ exact; brightness (15W linear @ max=100, tracked since today) = 5.48 Wh integrated; lifetime total 13.52 Wh matches sum of per-cap Wh from MQTT's concurrent discovery publishes.
- [x] Device health diagnostics (RSSI warnings, heap warnings)

## Microcontroller Library — Additional API

- [x] `setSwitch(id, value)` — update switch state from firmware
- [x] `setText(id, value)` — update text value from firmware
- [x] `setColor(id, value)` — update color value from firmware
- [x] `getSensor(id)` / `getSwitch(id)` — read current capability values
- [x] `setFirmwareVersion(version)` — custom firmware version string
- [x] `log(severity, message)` — send structured logs to desktop app
- [x] `logInfo()` / `logWarn()` / `logError()` — convenience log methods
- [x] `beginAutoConnect(timeout)` — WiFi provisioning via captive portal AP

## Infrastructure

- [x] GitHub Actions CI: build app (Linux)
- [x] GitHub Actions CI: compile library examples
- [x] GitHub releases with app binaries
- [x] Arduino Library Manager submission (arduino/library-registry#8088 merged 2026-04-07)
- [x] PlatformIO registry submission (nubiraorg/Trellis v0.1.8 published 2026-04-07)
- [x] Lean Arduino LM tarball — `library-release` orphan branch + `scripts/release-library.sh` + `.release-main-sha` dotfile resolver in `release.yml`. Drops the published Trellis-X.Y.Z.zip from ~740 KB / 122 files (entire monorepo) to ~50 KB / 25 files (library only) for v0.3.2 onward. Validated end-to-end at v0.3.2 (after fixing two release-infra bugs the prior session's dry-run missed: the tagged tree had no `.github/workflows/release.yml` so GitHub Actions couldn't fire on the tag, and reading the main SHA from the tag annotation didn't survive `actions/checkout@v4`'s fetch shape — switched to a `.release-main-sha` file). Old tags v0.1.8 → v0.3.1 stay bloated in the LM index (immutable).
- [x] **`TRELLIS_LOG` env var for desktop log file** (v0.17.0) — when Trellis is launched from the tray/menu its stderr goes to `/dev/null`, so `log::info!`/`warn!`/`error!` output was unreachable for post-hoc debugging. Setting `TRELLIS_LOG=1` writes logs to `~/.config/trellis/trellis.log` (append); `TRELLIS_LOG=<path>` writes to any literal path; unset keeps existing stderr behavior. Parent directory auto-created; falls back to stderr with a clear message on permission / path errors. When `TRELLIS_LOG` is active, log level defaults to `info` if `RUST_LOG` is unset so the file is useful without extra configuration. Implemented in `init_logging()` in `src-tauri/src/lib.rs` via `env_logger::Target::Pipe`.
